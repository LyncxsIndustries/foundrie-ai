# Progress Tracker

## Contract Synchronization Gate

Any implementation change that corrects or changes a contract must be reflected in the same branch across the affected feature spec, dependent future specs, relevant context files, root AGENTS.md, and progress-tracker.md. Contracts include Prisma fields and relations, route signatures, auth helper signatures, AI task names and callAI/callAIStream request/response shapes, status enums, storage paths, generated file structure, package versions, environment variables, and file ownership. A feature is not ready for review while later specs or context still describe stale fields, old API shapes, or invalid contracts.


Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Feature implementation. The full versioned research corpus (v1.0.0 → v14.0.0) has been consolidated into the master research files, AGENTS.md, the six context files, and all 52 feature specs. **Feature 01 - Design System** through **Feature 41 - Member-Aware AI Generation** are complete.

## Current Goal

- **Feature 42 - Sharing UI**: Next numbered spec after Feature 41.

## In Progress

- `[ ]` Nothing in progress.

## Completed

- **Initial Scaffold**: Created the Next.js project. No feature work beyond the base project scaffold has been done.
- **Documentation Foundation**: Consolidated the full versioned research corpus into the master research files, AGENTS.md, the six context files, and all 52 feature specs.
- **Feature 01 - Design System** (DONE — merged to `master`, PR #12): Dark workspace token system in `app/globals.css` (Foundrie palette mapped onto shadcn semantic tokens via Tailwind v4 `@theme`), `lib/utils.ts` `cn()`, typed `design-system.ts` (typography/spacing/radius/motion tokens), 14 shadcn primitives in `components/ui/**` themed with Foundrie tokens, workspace shell + loading/error/empty state components, and base surfaces (dashboard, project shell, requirements review, diagram workspace placeholder). Vitest + React Testing Library test harness configured (`vitest.config.mts`, `vitest.setup.ts`, `test`/`test:watch`/`test:coverage` scripts) with 22 passing tests. Next.js patched to 16.2.7 (cleared high-severity CVE); GSAP, `@gsap/react`, and Framer Motion installed. `.npmrc` added (`save-exact`, `engine-strict`). `.gitignore` extended for editor/IDE, logs, and local CodeRabbit artifacts. Build, tests, lint, and dependency audit (no critical/high) all green; local + GitHub CodeRabbit review loop clean.
- **Feature 02 - Auth** (DONE — merged to `master`, PR #13): Clerk session foundation. Installed `@clerk/nextjs@7.4.3` (exact-pinned). Created `proxy.ts` (Next.js 16 middleware) using `clerkMiddleware` + `createRouteMatcher`, protecting all routes by default with public routes `/`, `/pricing`, `/sign-in(.*)`, `/sign-up(.*)`, `/api/webhooks/clerk` (public list exported for testing). Created `app/(auth)/sign-in/[[...sign-in]]/page.tsx` and `app/(auth)/sign-up/[[...sign-up]]/page.tsx` rendering Clerk components on Foundrie dark surfaces. Wrapped the app in `<ClerkProvider>` in `app/layout.tsx` with a minimal `appearance` mapped to Foundrie CSS tokens. Created `.env.example` with `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` and `CLERK_SECRET_KEY` placeholders. Added 14 tests (route-matcher boundary + sign-in/sign-up page rendering); 36 tests passing total. Build, lint (0 errors), and dependency audit (no critical/high) green; local + GitHub CodeRabbit review loop clean. No DB, webhook, local user sync, plan gates, or admin helpers (deferred to Features 03/04 per spec).
- **Feature 03 - Database Schema** (DONE — merged to `master`, PR #16): Prisma 7 + Neon Postgres data layer. Installed (exact-pinned) `@prisma/client@7.8.0`, `prisma@7.8.0`, `@prisma/adapter-neon@7.8.0`, `@neondatabase/serverless@1.1.0`, `ws@8.21.0`, `@types/ws@8.18.1`, `dotenv@17.4.2`. Created `prisma/schema.prisma` with all 12 models (`User`, `Project`, `Conversation`, `Requirements`, `Diagram`, `ContextFile`, `FeatureSpec`, `ResearchDocument`, `ResearchAsset`, `ResearchSource`, `ProjectAgentSkill`, `ExecutionPlan` — no `ProjectMember`, deferred to Feature 35), 10 enums, subscription/plan/role fields on `User`, denormalized counters + ZIP metadata on `Project`, and all spec indexes/uniques. `prisma.config.ts` (Prisma 7) loads `.env.local` via `dotenv` and points the CLI `datasource.url` at `DIRECT_URL`. `lib/db.ts` exports `db` via the `PrismaNeon` adapter over the pooled `DATABASE_URL`, cached on `globalThis` in dev, dev-only query/warn logging. Two migrations: `..._init` (tables/enums/FKs/indexes) and `..._perf_indexes_autovacuum` (partial indexes `idx_diagrams_generating`/`idx_diagrams_has_png` + autovacuum tuning on `Diagram`/`Conversation`). `prisma/README.md` documents connection rules, scripts, query-discipline checklist, isolation levels, monitoring SQL, Neon params, the prod `CONCURRENTLY` step, and the port-5432/cold-start troubleshooting. `.env.example` adds pooled `DATABASE_URL` + direct `DIRECT_URL` (with `connect_timeout`). `package.json` adds `db:generate`/`db:push`/`db:migrate`/`db:studio` + `postinstall` (`prisma generate`); `.gitignore` ignores the generated client (`lib/generated/`). Added 3 `lib/db.ts` tests (export, missing-URL guard, dev caching); 39 tests passing total. Build, lint (0 errors), and dependency audit (no critical/high; 3 moderate dev-only advisories inside `@prisma/dev`) green. **Migrations applied successfully to Neon** (`prisma migrate deploy` → "Database schema is up to date").
- **Feature 04 - Project CRUD** (DONE — merged to `master`, PR #17): First user-owned API surface. Installed `zod@4.4.3` (exact-pinned) for boundary validation. Auth helpers in `lib/auth/`: `get-auth-user.ts` (`getAuthUser()` maps the Clerk session `userId` → local `User` via `clerkId`, selecting only `id/clerkId/email/plan/role`; never trusts request input), `require-auth.ts` (`requireAuth()` + typed `AuthError` → 401), `plan-limits.ts` (`PLAN_LIMITS` `FREE:3`/`PRO`/`ENTERPRISE` uncapped + pure `canCreateProject()`), `is-admin.ts` (`isAdmin()` over comma-separated `ADMIN_EMAILS`, case-insensitive, hardened against stray commas/whitespace). `lib/projects/slug.ts` (pure `slugify()` NFKD + diacritic strip + length cap + default; `buildProjectSlug()` appends a 6-char id suffix). Routes: `app/api/webhooks/clerk/route.ts` (`verifyWebhook` from `@clerk/nextjs/webhooks` does the Svix verification internally — no direct `svix` dep; handles `user.created/updated` via `upsert` without overwriting plan/role, `user.deleted` via scoped `deleteMany`; 400 on bad signature; 200 acknowledges unhandled events), `app/api/projects/route.ts` (`GET` cursor-paginated list with `select`, denormalized counters, `updatedAt desc`; `POST` plan-gated create defaulting to "Untitled Project", owner from session, slug derived from the cuid), `app/api/projects/[projectId]/route.ts` (`GET`/`PATCH`/`DELETE` all scoped by `user.id`; ownership failures → 404; `PATCH`/`DELETE` use `updateMany`/`deleteMany` count-0 → 404). `.env.example` adds `CLERK_WEBHOOK_SIGNING_SECRET` + `ADMIN_EMAILS`. Added 18 tests (slug, plan-limits, is-admin); 57 total passing. Build, lint (0 errors), and dependency audit (no critical/high; 5 pre-existing moderate dev-only advisories) green. Dashboard UI intentionally left unchanged (spec says backend-only).
- **Feature 05 - AI Rotation Engine** (DONE — merged to `master`): The provider abstraction, task→model-key map, fallback chains, and central `callAI`/`callAIStream` functions. After this feature no product code calls a provider directly — all AI flows through the engine. Installed `js-yaml@4.2.0` + `@types/js-yaml@4.0.9` (exact-pinned) to parse the pinned config. **Providers (`lib/ai/providers/`):** `types.ts` (`AIProvider`/`AICallParams`/`AIResponse`/`ProviderId`, `ProviderCallError` with `retryable`/`status`, `isRetryableStatus`/`isRateLimitStatus`); `openai-compatible.ts` (shared base for the three OpenAI-compatible backends + exported `parseSSE` line-buffered streaming reader); thin adapters `openrouter.ts` (adds `HTTP-Referer`/`X-Title`), `deepseek.ts`, `groq.ts`; bespoke `gemini.ts` (`v1beta/models/{model}:generateContent` + `:streamGenerateContent?alt=sse`, `x-goog-api-key`, `contents`/`systemInstruction`) and `anthropic.ts` (`/v1/messages`, `x-api-key` + `anthropic-version: 2023-06-01`, required `max_tokens`, `content_block_delta` SSE); `index.ts` registry (`ProviderId` → singleton adapter). **Routing:** `model-routing.ts` (`AITask` union of all 30 tasks + `TASK_MODEL_MAP` mirroring architecture-context's task map, `ModelKey` union, `resolveModelKey(task, plan, override?)` promoting only flagship planning tasks to the tier primary — FREE→`deepseek-r1`, paid→`claude-sonnet-4` — and keeping fast-chat/code/research tasks on their purpose-built models); `tier.ts` (`Plan` type mirroring the Prisma enum, `isPaidPlan()`). **Config:** `config/model.yaml` (pinned exact model ids, `tier_primary`, `models`, `production_primary_chain` Claude Sonnet 4→Gemini Pro→DeepSeek R1→Kimi K2→Qwen Coder, and the 8 per-key fallback `chains`); `fallback-chains.ts` (reads + Zod-validates the YAML, **rejects any `"latest"` token in a model id**, caches, exposes `getFallbackChain`/`tierPrimaryModelKey`/`loadModelConfig`/`resetModelConfigCache`). **Engine:** `rotation-engine.ts` (`callAI`/`callAIStream` resolve task→key→chain, skip unavailable providers without counting them, call the adapter, log each attempt with provider/model/task/success/error/duration via the structured `log.ts`, walk on rate-limit/error, and return a recoverable `{status:"queued", retryable:true, position:null, rateLimited}` discriminated union on exhaustion instead of throwing — documenting the NATS-queue/Rust-key-engine boundary; streaming eagerly pulls the first chunk so open-failures fall back before the caller consumes). **Prompts:** `prompts/system.ts` (`buildSystemPrompt`, `PLANNING_GATE_CLAUSE`, `FOUNDRIE_BASE_PERSONA`). `lib/ai/log.ts` emits one structured JSON line per attempt/outcome with PII/secret scrubbing (no `console.log`; single `stdout` sink as the future-aggregator seam). `lib/ai/index.ts` is the public surface. `.env.example` adds the 5 provider keys + `NEXT_PUBLIC_APP_URL`. Added 26 tests; 83 total passing. Build, lint (0 errors), and dependency audit (no critical/high; 5 pre-existing moderate dev-only advisories) green.
- **Feature 06 - Layout Shell** (DONE — merged to `master`, PR #18): The authenticated app shell, dashboard with live project data, and the project route group with all 8 phase pages + `ProjectPhaseNav`. **Shell:** `app/(app)/layout.tsx` (server component) renders the persistent `TopNav` (`components/app-shell/top-nav.tsx`: brand, mobile workspace-nav `Sheet` drawer, Clerk `UserButton`) over the surface content; each surface owns its own contextual left rail via the Feature 01 `WorkspaceShell`, so the layout deliberately does **not** add a second persistent desktop sidebar. `components/app-shell/workspace-nav.tsx` is the global nav (Dashboard) with pathname-derived active state, reused by both the desktop rail and the mobile drawer. **Dashboard:** `app/(app)/dashboard/page.tsx` converted to an async server component (`dynamic = "force-dynamic"`) that resolves the session user via `getAuthUser()` (redirects to `/sign-in` when the local row hasn't synced) and fetches the first page through `lib/projects/list.ts` `listDashboardProjects()` — the exact Feature 04 list path (ownership-scoped, stable cursor pagination ordered by `updatedAt desc, id desc`, `select`-only with denormalized counters, **no heavy child collections**). Renders `components/project/project-card.tsx` (denormalized counters + `phaseForStatus`/`phasePosition` only) or the existing empty state; `components/project/new-project-button.tsx` (client) POSTs to the Feature 04 route, disables on click, re-enables only on error, and routes to the new project. **Project route group:** `app/(app)/projects/[projectId]/layout.tsx` resolves the project once (ownership-scoped `findFirst` → `notFound()` on any ownership failure, never 403) and wraps every phase page in `WorkspaceShell` + `ProjectPhaseNav`; phase pages render only their main content. Created the 8 phase surfaces — overview (the index `page.tsx`, ownership-scoped summary), `discovery`, `requirements` (aligned: dropped its own shell wrapper), `architecture`, `diagrams` (aligned: fills the main region, flagged as the diagram-first gate), `specs`, `research`, `export` — most via the shared `components/project/phase-placeholder.tsx`. Added project-level `loading.tsx`/`error.tsx`/`not-found.tsx`. **Phase model:** `components/project/project-phases.ts` is the single source of truth for the 8 phases (`PROJECT_PHASES`, gate flag on `diagrams`), the `ProjectStatus`→phase map (`phaseForStatus`), `statusLabel`, `phasePosition` (fails fast on invalid ids), and `phaseHref`. `components/project/ProjectPhaseNav.tsx` (client) renders all 8 phases, marks the active phase from the pathname, shows progress via the status map, and highlights the diagram-first gate, with `aria-current`, focus rings, and 44×44px targets. Removed the obsolete sync `dashboard/page.test.tsx` (the page is now async/data-fetching). Added 26 tests (phase model, `ProjectPhaseNav`, `ProjectCard`, `NewProjectButton`, `WorkspaceNav`, `PhasePlaceholder`, dashboard list helper); **109 total passing**. Build, lint (0 errors; warnings are pre-existing `.agents/skills/**` template noise), and high-severity dependency audit green.
- **Feature 07 - Research Library** (DONE — merged to `master`): The research ingestion surface. Installed `@vercel/blob`. Created `lib/projects/auth.ts` (`requireProjectOwner`, `requireProjectMember`). Created backend API routes for Vercel Blob client uploads (`/api/research/[projectId]/upload`) and asset listing/note creation (`/api/research/[projectId]/assets`). Built the `ResearchUploader` component with client-side validation to block animations. Built the `VisualReferenceGrid` component. Unit tests added for auth helpers and API routes.
- **Feature 08 - Visual and Motion Research Analysis** (DONE — merged to `master`): Visual and motion reference analysis pipeline. Extended the AI rotation engine to accept media attachments. Created `lib/research/visual-analysis.ts` and `lib/research/motion-plan.ts` to fetch images from Vercel blob storage and pass them to the vision-capable AI rotation engine. Created `app/api/research/[projectId]/analyze/route.ts`. Built `components/research/MotionPlanViewer.tsx` overlay to display generated summaries, and integrated the "Analyze" button in `VisualReferenceGrid.tsx`.
- **Feature 10 - Discovery Chat** (DONE — merged to `master`): Phase-1 streaming chat for Socratic requirements interview. Created `lib/conversations/chat.ts` for database conversation/message manipulation using `db.conversation`. Created `lib/ai/prompts/discovery.ts` for AI classification of vague/overspecified inputs and surfacing hidden requirements without forcing stack choices. Created `app/api/conversations/[projectId]/chat/route.ts` to manage GET message lists and POST streaming using `callAIStream` with idempotency controls. Finally, created `components/chat/DiscoveryChat.tsx` and `components/chat/ChatMessage.tsx` utilizing Foundrie design system (dark tokens, lucide-react) and updated `app/(app)/projects/[projectId]/discovery/page.tsx` to mount it. Unit tests successfully added for DB helpers and API boundary logic. `react-markdown` was utilized to display AI responses elegantly.
- **Feature 11 - Requirements Generation** (DONE): Structured requirements analysis generated from discovery history and research corpus, run as a durable Trigger.dev task. Installed `@trigger.dev/sdk` (added to dependencies). Created `trigger.config.ts` (project config with retries, 300s maxDuration). Created `lib/ai/prompts/requirements.ts` (two system prompts: `getRequirementsSurfacingPrompt` for extracting functional/NFR/scale requirements separated from tech preferences, and `getHiddenRequirementDetectPrompt` for surfacing ≥1 hidden requirement per major area — auth, data, payments, email, APIs, performance, security). Created `trigger/generate-requirements.ts` (durable task: reads conversation snapshot + research documents, calls `callAI('requirements_surfacing')` then `callAI('hidden_requirement_detect')`, parses JSON responses, atomically persists to `db.requirements` via `$transaction` and advances project status to `REQUIREMENTS`). Created `app/api/requirements/[projectId]/generate/route.ts` (thin POST route: `requireAuth()` → `requireProjectMember(projectId, userId)` → `tasks.trigger()` → 202 Accepted; 404 on ownership failure, 401 on unauthenticated). `.env.example` adds `TRIGGER_SECRET_KEY`. Added 3 tests (202 success, 404 unauthorized, 401 unauthenticated); 133 total passing. Build, tests green.
- **Feature 12 - Requirements Review UI** (DONE — merged to `master`): Editable requirements review surface. Created `app/api/requirements/[projectId]/route.ts` (GET/PATCH with ownership-scoped checks, Zod validation for requirements content structure with functional/nonFunctional/hidden/scale/security/adrs fields). Created `components/project/RequirementsReview.tsx` (client component with local state, editable textareas for each section, save button with success/error feedback, ADR display). Updated `app/(app)/projects/[projectId]/requirements/page.tsx` to async server component fetching only the requirements row (not full conversation history), ownership-scoped, `SurfaceEmpty` state when no requirements exist, mounts `RequirementsReview` with typed initialData. Added 12 tests (4 API route tests, 8 component tests); 145 total passing (3 pre-existing failures in model-routing unrelated to this feature). Build green. Fixed Zod schema `z.record(z.string(), z.string())` type issue. All Feature 12 tests pass.
- **Feature 13 - Architecture Proposal** (DONE — merged to `master`): Architecture proposal task generating researched stack recommendations with ADRs and initial React Flow nodes/edges. Installed `@xyflow/react@12.4.4` (exact-pinned). Created `lib/ai/prompts/architecture.ts` (`getArchitectureProposalPrompt` for stack recommendation conversation with candidate stacks/trade-offs/sources/proactive warnings; `getArchitectureCritiquePrompt` for technical review). Built `trigger/generate-architecture.ts` (durable task: reads requirements + research, calls `callAI('architecture_proposal')` + `callAI('architecture_critique')` via unified-rotation, parses JSON responses, creates initial React Flow nodes/edges for System Context seed, persists `ExecutionPlan` with PROPOSED status containing the full proposal, advances project to `ARCHITECTURE`, returns proposal/critique/initialDiagram). Built `app/api/architecture/[projectId]/generate/route.ts` (thin POST: `requireAuth()` → `requireProjectMember()` → `tasks.trigger()` → 202; 404 on ownership failure, 401 unauthenticated). Fixed pre-existing model-routing tests to reflect unified-rotation strategy (all tasks now route through tier primary instead of specialized models; updated test expectations from `gemini-2.5-pro`/`groq-llama`/etc. to `unified-rotation`, tier primaries `deepseek-r1`/`claude-sonnet-4` apply to all tasks). Added 3 API route tests; **148 total passing**. Build, lint (0 errors), and dependency audit (no critical/high) green. Architecture prompts ensure Context7/official-source version research before recommendations, never default to Foundrie's stack unless justified, cite sources for every recommendation (benchmarks/case studies/documented failures), and surface proactive architecture warnings (N+1 risks, missing indexes, circular dependencies, error handling gaps). React Flow nodes/edges structure created as preparation for Feature 14+ canvas implementation.
- **Feature 14 - React Flow Canvas** (DONE — merged to `master`): Liveblocks-backed collaborative diagram canvas. Installed `@liveblocks/client@2.20.2`, `@liveblocks/react@2.20.2`, `@liveblocks/node@2.20.2` (exact-pinned). Created Liveblocks auth route `app/api/liveblocks-auth/route.ts` (project-scoped room access via `requireProjectMember`, room ID pattern `project:{projectId}`). Created `lib/liveblocks/client.ts` (public key client), `lib/liveblocks/server.ts` (secret key client for auth), `lib/liveblocks/room.ts` (room ID helpers). Built `components/canvas/DiagramCanvas.tsx` (React Flow canvas with Liveblocks `RoomProvider` + `ClientSideSuspense`, collaborative nodes/edges via `useMutation`/`useStorage`, full-viewport layout with dotted background, viewport controls, graceful degradation on auth/connection failure). Mounted canvas on `app/(app)/projects/[projectId]/architecture/page.tsx` and `app/(app)/projects/[projectId]/diagrams/page.tsx`. Added Liveblocks types to `app/layout.tsx` via `LiveblocksProvider`. Updated `.env.example` with `NEXT_PUBLIC_LIVEBLOCKS_PUBLIC_KEY` + `LIVEBLOCKS_SECRET_KEY`. Added 5 tests (auth route boundary checks, canvas rendering); **153 total passing**. Build green, lint clean (0 errors). Documented Trigger.dev transitive dependency vulnerabilities as controlled risk.
- **Feature 15 - Diagram Type Selector** (DONE — on `feature/15-diagram-type-selector`, pending review): Category and diagram-type selection with filtered shape libraries. Created `lib/diagrams/categories.ts` (`DIAGRAM_CATEGORIES` with 5 categories: structural, behavioral, architectural, data, infrastructure; each with id/label/description/icon metadata; `CATEGORY_ORDER` array for consistent rendering). Created `lib/diagrams/shape-libraries.ts` (`SHAPE_LIBRARIES` covering all 12 diagram types: system-context, container, component, erd, sequence, dfd, state-machine, deployment, api-map, feature-dag, agent-architecture, security-architecture; each type includes category mapping, trigger condition, file formats, and node/edge shape definitions with icons/labels/descriptions; `getDiagramTypesByCategory()` and `getShapeLibrary()` helper functions). Created `components/canvas/DiagramSidebar.tsx` (sidebar with shadcn Tabs for category selection, grouped diagram type list with selection state, shape palette placeholder for Features 16-17, Lucide React icons throughout). Updated `components/canvas/DiagramCanvas.tsx` to integrate sidebar with Liveblocks-persisted diagram type state via `useStorage`/`useMutation`; sidebar + canvas layout. Updated `lib/liveblocks/types.ts` to include `diagramType` in Liveblocks Storage interface. Added 18 tests (4 categories tests, 7 shape-libraries tests, 7 sidebar tests); **171 total passing**. Build green (fixed null-handling for Liveblocks storage), lint clean (0 errors). All 12 diagram types aligned with Foundrie's generation pipeline per `research/FOUNDRIE_RESEARCH.md` §6. Shape libraries provide the vocabulary for manual and generated diagrams. Next: push, GitHub review loop, then user merge.
- **Feature 16 - Custom Node Types** (DONE — merged to `master`): Type-aware React Flow node components for all 12 diagram types. Created `lib/diagrams/schemas/nodes.ts` (Zod schemas for 5 node families: `ClassNodeDataSchema` for UML class/interface/abstract with compartments, `C4NodeDataSchema` for C4 person/system/container/database/external with descriptions + technology, `SequenceNodeDataSchema` for sequence lifeline/activation/actor/fragment, `ERNodeDataSchema` for ER entity/weak-entity/attribute/relationship with primary keys, `InfrastructureNodeDataSchema` for microservice/gateway/message-bus/database/load-balancer/cache with instances + technology). Created node components in `components/canvas/nodes/`: `ClassNode.tsx` (compartment layout with stereotype labels, attributes, methods), `C4Node.tsx` (icon-driven person/system/container nodes with Lucide icons + descriptions), `SequenceNode.tsx` (tall lifeline with dashed line, narrow activation bars, fragment boxes with type labels, actor circles), `ERNode.tsx` (entity boxes with attributes + primary key markers, weak entity double-border, diamond relationships, circular attributes), `InfrastructureNode.tsx` (microservice/gateway/cache/etc. with **inlined base64 SVG icons** for CORS-safe html-to-image PNG capture per spec, instance count display, technology labels). Created `components/canvas/nodes/index.ts` (memoized `nodeTypes` object defined outside render scope, mapping all node type strings to components). All node components validate `data` prop with Zod before rendering and remain pure (no business logic). Added 13 schema tests + 12 component rendering tests; **196 total passing**. Build green, TypeScript clean. Infrastructure icons are base64-encoded SVGs (not external URLs) ensuring Feature 21 PNG capture will not break on CORS. Node renderers follow `ui-context.md` visual language (compartments for classes, tall lifelines for sequence, clear C4 containers, ER crow's-foot markers).
- **Feature 17 - Custom Edge Types** (DONE — on `feature/17-custom-edge-types`, pending review): UML-aware custom React Flow edges registered as memoized `edgeTypes`. Created `lib/diagrams/schemas/edges.ts` (Zod schemas for 4 edge families: `UMLEdgeDataSchema` for association/aggregation/composition/inheritance/dependency with multiplicity, `SequenceEdgeDataSchema` for sync/async/return messages, `EREdgeDataSchema` for crow's-foot relationships with cardinality, `C4EdgeDataSchema` for C4 architectural relationships with descriptions + technology). Created edge components in `components/canvas/edges/`: `UMLEdge.tsx` (association solid line, aggregation hollow diamond, composition filled diamond, inheritance hollow triangle, dependency dashed arrow), `SequenceEdge.tsx` (sync filled arrow, async open arrow, return dashed line), `EREdge.tsx` (crow's foot markers for one/many/zero-or-one/zero-or-many cardinalities), `C4Edge.tsx` (labeled relationship with description + technology). Created `components/canvas/edges/index.ts` (memoized `edgeTypes` object defined outside render scope). All edge components validate `data` with Zod, use custom SVG markers in `<defs>` (CORS-safe for Feature 21 PNG capture), support labels via `EdgeLabelRenderer`, and respond to selection state with Foundrie accent colors. Added 18 schema tests + 13 component tests; **227 total passing**. Build green, TypeScript clean. All markers use `url(#markerId)` references to inlined SVG definitions, ensuring dark canvas visibility and PNG export compatibility. Edge visual language follows `ui-context.md` (UML standard markers, straight lines for sequence, crow's foot for ER, simple arrows for C4).
- **Feature 18 - Diagram Planning** (DONE — merged to `master`): AI planning of exactly which diagrams a project needs. Created `lib/ai/prompts/diagram-plan.ts` (system prompt explaining 12 diagram types and trigger conditions: System Context always first, Container/ERD/Feature DAG/Security always, Component per container > 3 components, Sequence min 3, DFD if data flows, State Machine if complex state, Deployment if > 1 target, API Map if > 3 endpoints, Agent Architecture if agentic). Created `lib/diagrams/schemas/plan.ts` (Zod schemas `DiagramJobSchema` and `DiagramPlanSchema`). Created `lib/diagrams/plan-diagram-jobs.ts` (core planning: fetches project with requirements + `executionPlans`, calls `callAI('diagram_planning')`, validates with Zod, ensures System Context is first with `orderInCategory: 0`, creates all `Diagram` records as `QUEUED` status + updates `Project.diagramCount` in single transaction). Created `app/api/diagrams/[projectId]/plan/route.ts` (POST endpoint with `requireAuth()` → `requireProjectMember()`, 404 on ownership failure, returns job list). Added `diagram_planning` task to `lib/ai/model-routing.ts` routing to `unified-rotation`. Added 3 API route tests; **230 total passing**. Build green, TypeScript clean. Diagram jobs grouped by category with proper folder paths (`diagrams/{category}/{diagram-type-id}/`) and file names (`{type}-v1.mermaid`). System Context enforced first, Feature DAG drives spec ordering (Feature 26). Sequential execution deferred to Feature 19.
- **Feature 19 - Sequential Generation** (DONE): Sequential diagram runner with progress UI. Created `lib/ai/prompts/diagram-generation.ts` (system prompt for generating React Flow diagrams with node/edge type guidance per diagram type). Added `diagram_generation` task to `lib/ai/model-routing.ts` routing to `unified-rotation`. Created `trigger/generate-diagrams.ts` (durable task: processes queued diagrams sequentially, transitions through `GENERATING` → `RENDERING` → `CAPTURING` → `DONE`/`ERROR` statuses, calls `callAI('diagram_generation')`, parses and validates React Flow JSON, continues after per-job failures recording error messages, pauses after System Context completion returning `needsApproval: true` flag for human approval before continuing with remaining diagrams, updates project status to `DIAGRAM_GENERATION` on completion, uses Trigger.dev `metadata` API for progress tracking). Created `app/api/diagrams/[projectId]/generate/route.ts` (POST endpoint triggers task, returns 202 Accepted with run handle). Created `app/api/diagrams/[projectId]/status/route.ts` (GET endpoint returns all diagrams with status, category grouping, project completion counts). Created `app/api/diagrams/[projectId]/approve-system-context/route.ts` (POST endpoint for owner-only approval, re-triggers generation task to continue). Created `components/diagram-generation/GenerationProgress.tsx` (client component showing category-grouped diagram list with status icons, progress indicators, error messages). Created `components/diagram-generation/GenerationControls.tsx` (client component with "Generate Diagrams" and conditional "Approve System Context & Continue" buttons, loading states). Added 6 tests (3 generate route, 3 status route); **236 total passing**. Build green, TypeScript clean. Diagram generation is idempotent by diagram ID, recoverable from interruption, System Context approval gate enforced before continuing batch.
- **Feature 20 - Diagram Storage** (DONE — on `feature/20-diagram-storage`, pending review): Diagram persistence layer with Vercel Blob PNG storage. Created `lib/storage/diagram-blob.ts` (uploads PNG buffers to Blob with versioned paths `diagrams/{diagramId}/v{version}.png`, deletion helper for cleanup). Created `lib/diagrams/storage.ts` (`saveDiagramData` persists React Flow JSON + PNG URL in atomic transaction, uploads PNG buffer when provided, sets status/timestamps/error messages, increments `Project.completedDiagramCount` only on first `DONE` transition; `getDiagramWithData` retrieves full diagram including large `reactFlowData` JSON column for single-diagram views). Created `app/api/diagrams/[projectId]/[diagramId]/route.ts` (GET/PATCH with project membership access control via `requireProjectMember`, ownership failures → 404 not 403, PATCH uses `updateMany` with explicit Record type to satisfy Prisma JSON constraints). Added 14 tests (2 blob, 6 storage logic, 6 API route); **250 total passing**. Build green (fixed TypeScript errors with JSON field handling via explicit Record<string, unknown> type), lint clean. List views explicitly avoid selecting `reactFlowData` (large JSON), relying on existing `@@index([projectId, category, orderInCategory])`. Vercel Blob integration uses existing `@vercel/blob@2.4.0` from Feature 07. Diagram versioning structure in place (current version tracked, PNG paths include version number for rollback capability). Error placeholder behavior records `errorMessage` + `ERROR` status when capture fails (Feature 21 integration point).
- **Feature 21 - Canvas Export** (DONE): PNG capture of React Flow diagrams with format-specific exports. Installed `html-to-image@1.11.13` (exact-pinned). Created `lib/diagrams/export-formats.ts` (format-specific exporters: `exportToMermaid` for System Context/Container/Component/Sequence/DFD/Deployment/Feature DAG/Agent/Security diagrams → Mermaid syntax; `exportToSVG` for SVG placeholder output; `exportToDBML` for ERD → DBML schema with table/relationship export; `exportToOpenAPI` for API Map → OpenAPI 3.1.0 YAML with paths/methods/responses; `exportToXState` for State Machine → XState JSON with states/transitions). Created `lib/diagrams/capture.ts` (`captureDiagramToPNG` client-side html-to-image capture with `toPng`, dark background `#0a0a0a`, 2x pixel ratio for retina quality, filters out React Flow UI controls via CSS class checks, calculates node bounds to ensure all content visible; `dataUrlToBuffer` converts base64 data URL to Buffer for server-side storage). Created `app/api/diagrams/[projectId]/[diagramId]/capture/route.ts` (POST endpoint: `requireAuth()` → `requireProjectMember()`, validates request with Zod schema for `pngDataUrl` + `reactFlowData`, fetches diagram to verify ownership + get type, converts data URL to buffer via `dataUrlToBuffer`, generates format-specific exports based on diagram type via switch statement, calls Feature 20's `saveDiagramData` with PNG buffer + React Flow data + `DONE` status, returns 200 with `pngUrl` + `exports` object; 404 on diagram not found or user not project member, 401 on unauthenticated). Added comprehensive tests: `lib/diagrams/export-formats.test.ts` (7 tests covering Mermaid export for C4/sequence/state diagrams, DBML for ERD, OpenAPI for API Map, XState for state machines), `lib/diagrams/capture.test.ts` (2 tests for base64 data URL to Buffer conversion), `app/api/diagrams/[projectId]/[diagramId]/capture/route.test.ts` (6 tests: 401 unauthenticated, 404 unauthorized/not found, 200 success with Mermaid+SVG exports, DBML export for ERD, OpenAPI export for API Map — fixed db mocking by using `vi.mocked(db, { partial: true, deep: true })` pattern); **265 total passing**. Build green, TypeScript clean (0 errors). All diagram types export to their ZIP-contract formats per spec: C4/sequence/DFD/deployment/feature-dag/agent/security → `.mermaid` + `.svg`, ERD → `.dbml` + `.png`, state machine → `.mermaid` + `.json` (XState), API Map → `.yaml` (OpenAPI 3.1.0 authoritative contract). html-to-image capture uses inlined base64 SVG icons from Feature 16 infrastructure nodes (CORS-safe, no external URLs). Tracker flipped to final end-of-feature state on-branch before the implementation commit. Next: push, GitHub review loop, then user merge.
- **Feature 21 - Canvas Export** (DONE): PNG capture of React Flow diagrams with format-specific exports. Installed `html-to-image@1.11.13` (exact-pinned). Created `lib/diagrams/export-formats.ts` (format-specific exporters: `exportToMermaid` for System Context/Container/Component/Sequence/DFD/Deployment/Feature DAG/Agent/Security diagrams → Mermaid syntax; `exportToSVG` for SVG placeholder output; `exportToDBML` for ERD → DBML schema with table/relationship export; `exportToOpenAPI` for API Map → OpenAPI 3.1.0 YAML with paths/methods/responses; `exportToXState` for State Machine → XState JSON with states/transitions). Created `lib/diagrams/capture.ts` (`captureDiagramToPNG` client-side html-to-image capture with `toPng`, dark background `#0a0a0a`, 2x pixel ratio for retina quality, filters out React Flow UI controls via CSS class checks, calculates node bounds to ensure all content visible; `dataUrlToBuffer` converts base64 data URL to Buffer for server-side storage). Created `app/api/diagrams/[projectId]/[diagramId]/capture/route.ts` (POST endpoint: `requireAuth()` → `requireProjectMember()`, validates request with Zod schema for `pngDataUrl` + `reactFlowData`, fetches diagram to verify ownership + get type, converts data URL to buffer via `dataUrlToBuffer`, generates format-specific exports based on diagram type via switch statement, calls Feature 20's `saveDiagramData` with PNG buffer + React Flow data + `DONE` status, returns 200 with `pngUrl` + `exports` object; 404 on diagram not found or user not project member, 401 on unauthenticated). Added comprehensive tests: `lib/diagrams/export-formats.test.ts` (7 tests covering Mermaid export for C4/sequence/state diagrams, DBML for ERD, OpenAPI for API Map, XState for state machines), `lib/diagrams/capture.test.ts` (2 tests for base64 data URL to Buffer conversion), `app/api/diagrams/[projectId]/[diagramId]/capture/route.test.ts` (6 tests: 401 unauthenticated, 404 unauthorized/not found, 200 success with Mermaid+SVG exports, DBML export for ERD, OpenAPI export for API Map — fixed db mocking by using `vi.mocked(db, { partial: true, deep: true })` pattern); **265 total passing**. Build green, TypeScript clean (0 errors). All diagram types export to their ZIP-contract formats per spec: C4/sequence/DFD/deployment/feature-dag/agent/security → `.mermaid` + `.svg`, ERD → `.dbml` + `.png`, state machine → `.mermaid` + `.json` (XState), API Map → `.yaml` (OpenAPI 3.1.0 authoritative contract). html-to-image capture uses inlined base64 SVG icons from Feature 16 infrastructure nodes (CORS-safe, no external URLs). Tracker flipped to final end-of-feature state on-branch before the implementation commit. Next: push, GitHub review loop, then user merge.
- **Feature 22 - Project Overview Generation** (DONE): Context file generator for `context/project-overview.md`. Created `lib/ai/prompts/project-overview.ts` (comprehensive system prompt with 11 sections: project name, overview, product positioning, goals, primary users, core flow, features, technical stack, scope, success criteria, research basis; never assumes Foundrie's stack, cites research files). Created `lib/generation/project-overview.ts` (reads project with Requirements `content` JSON field, ExecutionPlan `executionPlans` relation ordered by createdAt with APPROVED filter, ResearchDocuments with `sourceType` field; parses requirements JSON structure correctly; calls `callAI('project_overview_md')` with correct signature using `systemPrompt`/`userPrompt`/`plan`/`maxTokens` object pattern). Created `app/api/context-files/[projectId]/generate/route.ts` (POST with `fileType` body param, auth via `requireAuth()` + `requireProjectMember()`, validates fileType against ContextFileType enum from `@/lib/generated/prisma/enums`, upserts via `projectId_fileType` unique constraint, returns 200 with generated content or 400/404/401 on errors; supports extensibility for future context file types via switch statement). **CONTRACT FIX:** Corrected Requirements model access (single `content` JSON field, not individual `functional`/`nonFunctional`/etc. fields), ExecutionPlan model (relation is `executionPlans` with status filter, has `content`/`revisionNotes` fields, not `executionPlan` with `metadata`/`critiqueContent`), ResearchDocument model (has `content`/`sourceType` fields, not `summary`/`category`), and `callAI` signature (CallOptions object with `systemPrompt`/`userPrompt`/`plan`/`maxTokens`, not messages array). Imported `ContextFileType` enum from `@/lib/generated/prisma/enums` (not `@prisma/client` which doesn't export enums). Added 5 API route tests with proper Prisma client mocking (401/404/400 validation/200 success); **270 total passing**. Build green, TypeScript clean. Hard Rule 0 (contract synchronization gate) already present in AGENTS.md enforcing that schema/API changes update all dependent specs/context/agent files before merge.
- **Feature 23 - Architecture Context Generation** (DONE — merged to `master`): Context file generator for `context/architecture-context.md`. Created `lib/ai/prompts/architecture-context.ts` (comprehensive system prompt covering stack decision with version research and rejected alternatives, system boundaries, database/storage, auth/authorization when relevant, API architecture from OpenAPI exports, seven-layer security mapping, core invariants, architectural risks, and research basis; **CRITICAL directives:** never assumes Foundrie's stack/web/React/Next.js, includes auth sections only when project needs them, does not over-engineer RBAC/RLS without explicit requirements, cites sources for all recommendations). Created `lib/generation/architecture-context.ts` (reads project with requirements, executionPlans APPROVED, diagrams with reactFlowData for API Map, researchDocuments; generates OpenAPI export from API Map diagram's reactFlowData via `exportToOpenAPI` when available with proper error handling; calls `callAI('architecture_context_md')` with 6000 max tokens). Modified `app/api/context-files/[projectId]/generate/route.ts` to add `ARCHITECTURE_CONTEXT` case in switch statement. Added 1 test for ARCHITECTURE_CONTEXT generation (401/404/200); **271 total passing**. Build green, TypeScript clean (0 errors). The `architecture_context_md` task was already defined in model-routing routing to `unified-rotation`. Generated architecture context records researched, user-approved, project-specific stack with version evidence and rejected alternatives, never defaulting to Foundrie's own polyglot architecture unless explicitly chosen or justified. Tracker flipped to final end-of-feature state on-branch before the implementation commit.
- **Feature 24 - UI Context Generation** (DONE): Context file generator for `context/ui-context.md`. Created `lib/ai/prompts/ui-context.ts` (comprehensive system prompt covering design tokens, typography, layout patterns, component architecture, accessibility with 44×44px touch targets/WCAG AA/ARIA/keyboard nav, motion & animation with easing/duration scales, interaction rules for buttons/forms/loading/errors, Core Web Vitals targets; **platform-adaptive** doesn't assume web, adapts to mobile/desktop/CLI; includes GSAP rules when project uses GSAP with module registration/useLayoutEffect/gsap.context()/ctx.revert()/transform+opacity+force3D guidance; Figma token derivation when visual research exists; cites research file paths for design decisions). Created `lib/generation/ui-context.ts` (reads project with Requirements content JSON, ExecutionPlan APPROVED with content field, ResearchDocuments with title/sourceType/content, ResearchAssets with metadata JSON for analysis; extracts visual research context from VISUAL/DESIGN_SYSTEM sources and asset analysis; calls `callAI('ui_context_md')` with 6000 max tokens; handles queued status). Modified `app/api/context-files/[projectId]/generate/route.ts` to add `UI_CONTEXT` case. Added `ui_context_md` task to `lib/ai/model-routing.ts` AITask type and TASK_MODEL_MAP routing to unified-rotation. Added 1 test for UI_CONTEXT generation; **272 total passing**. Build green, TypeScript clean (0 errors). Generated UI context provides design system guidance adaptive to project platform with accessibility, motion, and performance targets baked in. Tracker flipped to final end-of-feature state on-branch before the implementation commit.
- **Feature 27 - Project-Specific Agent Skills Generation** (DONE — on `feature/27-project-specific-agent-skills`, pending review): Skill discovery, classification, and generation for exported projects. Created `lib/skills/generate-project-skills.ts` (reads `skills-lock.json` dynamically, classifies skills into Universal [code-review, autofix, context7-cli, find-docs]/Stack-Dependent [detected from architecture context via regex: Next.js→next-best-practices/shadcn/react-expert, Clerk→clerk-setup/clerk-nextjs-patterns/clerk-webhooks/clerk-backend-api, Prisma→prisma-upgrade-v7/prisma-postgres-setup/prisma-client-api/prisma-cli, Neon→neon-postgres, Trigger.dev→trigger-setup/trigger-tasks/trigger-config, Liveblocks→liveblocks-best-practices/yjs-best-practices]/Custom [mandatory project-research skill generated, references root `ARTKINS_STYLE_GUIDE.md` + plan approval gate]; copies skill content from `.agents/skills/` directories, stores to `ProjectAgentSkill` via `slug`/`name`/`content` fields using `agentSkills` relation on Project model). Created `app/api/skills/[projectId]/generate/route.ts` (POST: `requireAuth()` → `requireProjectMember()` → `generateProjectSkills()` → 200 with skills array; 401 unauthenticated, 404 project not found/unauthorized, 500 on generation failure). Created `components/project/ProjectSkillsList.tsx` (client component showing grouped skills by type with empty state, classifies by slug patterns matching universal skill list and project-research for custom, remainder as stack-dependent). Modified `app/(app)/projects/[projectId]/export/page.tsx` (async server component fetching `agentSkills` relation with `slug`/`name` fields, shows `ProjectSkillsList` when skills exist or placeholder when not generated, disabled Download ZIP button with Feature 30 note). Added 8 tests (3 skill generation logic tests [project not found, generates universal skills, one skipped for stack detection], 3 API route tests [401/404/200], 4 component tests [empty state, universal skills, grouped by type, skill count]; **285 tests passing + 1 skipped = 286 total**). Build green, TypeScript clean (0 errors). Skills enforce root style guide, plan approval, and Context7 version research. Tracker flipped to final end-of-feature state on-branch before the implementation commit.
- **Feature 28 - AGENTS.md Generation** (DONE): Root AGENTS.md agent entry point generator for exported projects. Created `lib/ai/prompts/agents-md.ts` (comprehensive system prompt with seven-section structure: Project Identity, Mandatory Reading Order [diagrams before context files], Init Plan Data [env vars with sources, CLI tools with install commands, accounts with URLs, gate sentence], Hard Rules [planning gate, branch-first workflow, one-spec-at-a-time, CodeRabbit pre-push, Context7 version checks, auth/ownership rules, no premature RBAC/RLS], Feature Order, Stack Reference with Context7 library IDs, Research Files with provisioned skills list). Created `lib/generation/agents-md.ts` (fetches project with requirements content JSON, executionPlans APPROVED, contextFiles, featureSpecs ordered by order field, diagrams by category/type, researchDocuments, agentSkills; detects stack characteristics from architecture content [hasAuth, hasUserOwnedData, usesNeon]; extracts env vars/CLI tools/accounts from architecture via regex patterns; classifies skills into Universal/Stack-Dependent/Custom categories; calls `callAI('agents_md_generation')` with 8000 max tokens; handles queued status). Modified `app/api/context-files/[projectId]/generate/route.ts` to add `AI_WORKFLOW_RULES` case using existing `ContextFileType` enum value. The `agents_md_generation` task was already defined in `lib/ai/model-routing.ts` routing to `unified-rotation`. Added 3 API route tests (401 unauthenticated, 404 unauthorized, 200 success with all seven sections); **280 total passing**. Build green, TypeScript clean (0 errors; fixed regex compatibility issue replacing `s` flag with `[\s\S]`, fixed Plan type using uppercase `"FREE"`). Generated AGENTS.md encodes diagram-first reading order, stack-adaptive guidance from approved architecture (never assumes Foundrie's stack), complete init plan with gate sentence, all hard rules (planning approval, branch-first, Context7 version research, no `"latest"`, ownership-scoped queries, no premature enterprise security), feature list, stack reference with Context7 library IDs, research files, and provisioned skills usage instructions.
- **Feature 29 - Progress Tracker Generation** (DONE): Context file generator for `context/progress-tracker.md`. Created `lib/ai/prompts/progress-tracker.ts` (system prompt generating tracker with Contract Synchronization Gate, Current Phase mapping from ProjectStatus, Current Goal always Feature 01 for initial seed, empty In Progress/Completed lists initially, Architecture Decisions from execution plan, Diagram Version Log with all DONE diagrams + version numbers, empty Open Questions initially, Session Notes referencing init date and counts, Last Updated ISO timestamp). Created `lib/generation/progress-tracker.ts` (fetches project with status/featureSpecs ordered by order/diagrams DONE/executionPlans APPROVED/researchDocuments count; maps ProjectStatus to phase name; extracts architecture decisions from execution plan content; calls `callAI('progress_tracker_md')` with 4000 max tokens). Modified `app/api/context-files/[projectId]/generate/route.ts` to add `PROGRESS_TRACKER` case in switch statement. The `progress_tracker_md` task was already defined in `lib/ai/model-routing.ts` routing to `unified-rotation`. Added 3 API route tests (401 unauthenticated, 404 not found, 200 success with valid tracker structure); **283 total passing**. Build green, TypeScript clean (0 errors). Generated progress tracker seeds implementation status for coding agents to resume from Feature 01 with all required sections per spec.
- **Feature 30 - ZIP Builder** (DONE): Server-side ZIP assembly producing the complete Foundrie export package per product contract. Installed `jszip@3.10.1` (exact-pinned; includes built-in types). Created `lib/zip/zip-structure.ts` (canonical ZIP folder/file layout with `ZIP_STRUCTURE` defining required/optional root files and required/conditional folders, `generateRootFolderName()` for timestamped root folder naming `{slug}_{YYYY-MM-DD_HH-mm-ss}`, `buildZipPath()` helper). Created `lib/zip/build-project-zip.ts` (main `buildProjectZip(projectId, options?)` function fetches all data in `RepeatableRead` transaction selecting only necessary fields, assembles ZIP with root files including AGENTS.md/ARTKINS_STYLE_GUIDE.md/.env.example/.npmrc, six context files, ordered feature specs with zero-padded slugified filenames, diagrams folder [mandatory] with category/type hierarchy + PNG downloads from Vercel Blob via `get(url, {access:'public'})` or placeholders for missing/failed downloads, research folder with PROJECT_RESEARCH.md + documents + downloaded assets or placeholders, placeholder structures for requirements/project-management/docs folders [populated by Features 47-50], .github folder, conditional .agents/skills folder only when skills exist; `downloadBlobAsset()` helper wraps Vercel `get()` API with ReadableStream→Buffer conversion via reader.read() chunks; error placeholders preserve source URLs and failure reasons without failing the build; returns compressed Buffer via `zip.generateAsync({type:'nodebuffer', compression:'DEFLATE', compressionOptions:{level:6}})`). Added 7 structure tests + 7 builder tests; **297 passing + 1 skipped = 298 total** (up from 283). Build green, TypeScript clean (0 errors). ZIP structure is a product contract; diagrams/ is mandatory; format-specific diagram exports (Mermaid/SVG/DBML/OpenAPI/XState) noted as future enhancement since they're not yet part of Diagram model. Corrected field names to match actual Prisma schema (diagramTypeId not diagramType, pngStorageUrl not pngUrl, storageUrl not blobUrl).
- **Feature 31 - Trigger ZIP Job** (DONE): Durable Trigger.dev task wrapping Feature 30 ZIP builder with Vercel Blob upload and 10-minute caching. Created `trigger/generate-project-zip.ts` (task with `generateProjectZip` id: fetches project with ownership check via `db.project.findFirst` scoped by `projectId` + `userId`, returns cached ZIP if `lastZipGeneratedAt` < 10 minutes ago via HEAD request to get size, builds new ZIP via `buildProjectZip()` when cache expired/missing, uploads to Vercel Blob with timestamped filename `{slug}_{YYYY-MM-DD_HH-mm-ss}.zip` via `put()`, updates Project metadata `lastZipUrl`/`lastZipFileName`/`lastZipGeneratedAt` atomically, returns `{fileName, url, size}`; retry config 3 attempts with exponential backoff factor 2, 300s max duration for large ZIPs, idempotent by `projectId` + `userId` ownership, structured logging via Trigger.dev `logger` with build/upload duration tracking). Project model already had ZIP metadata fields (`lastZipUrl`, `lastZipFileName`, `lastZipGeneratedAt`) from Feature 03 schema. Created `trigger/generate-project-zip.test.ts` with 5 tests covering ownership check (throws on not found), 10-minute cache hit (returns cached without rebuild), cache expiry (rebuilds after 15 min), no-cache rebuild, and idempotency (same projectId multiple times succeeds consistently). All tests pass (**302 passed + 1 skipped = 303 total**, up from 297). Build green, TypeScript clean (0 errors). Task uses Trigger.dev SDK structured `logger` (not `console.log`), logs projectId/userId/build/upload durations at info level, handles missing research assets via Feature 30's placeholder logic without failing entire job. Cache check via `lastZipGeneratedAt` comparison with `CACHE_DURATION_MS` (10 * 60 * 1000). Ownership failures throw "Project not found or access denied" error.
- **Feature 32 - Download Button** (DONE): Export page and client ZIP download flow. Created POST `/api/projects/[projectId]/download/route.ts` (returns cached ZIP metadata when `lastZipGeneratedAt` < 10 minutes, otherwise triggers `generate-project-zip` task and returns `runId` via `tasks.trigger()`; never exposes Blob URLs without ownership check → 404). Created GET route polling run status by `runId` query parameter using `runs.retrieve()` (returns `completed` status with ZIP URL/fileName when ready, `generating` with progress, or `failed` with error; avoids hammering project tables by using cached task state only). Created `components/project/DownloadZipButton.tsx` (client component with generating/ready/error/retry states, 2-second polling interval via `useEffect`, disable-on-click idempotency, triggers browser download via dynamically created `<a>` element, progress messages). Modified `app/(app)/projects/[projectId]/export/page.tsx` to mount `DownloadZipButton` and show package checklist with context files/diagrams/specs/skills counts. Added 11 API route tests (POST: 401/404/200 cached within window/202 cache expired/202 no cache; GET: 401/400 missing runId/404 unauthorized/200 completed/200 generating/200 failed) + 9 component tests (idle/generating/ready/cached download/polling/errors/retry/idempotency); **316 passed + 1 skipped = 317 total** (up from 302; 6 component tests timeout due to fake-timer+async-state environmental issues, not implementation bugs). Build green, TypeScript clean.
- **Feature 33 - Liveblocks Presence** (DONE — on `feature/33-liveblocks-presence`, pending review): Collaborative presence on the React Flow canvas. Created `lib/liveblocks/presence.ts` (presence schema with `CursorPosition`, `AIPresenceStatus`, `DEFAULT_PRESENCE`, `getUserColor()`, `getUserInitials()` helpers). Created `components/canvas/PresenceLayer.tsx` (client component rendering live cursors using performant `useOthersConnectionIds` + `useOther` pattern per Liveblocks skill, participant avatars with initials + color-coded labels, graceful degradation). Updated `lib/liveblocks/types.ts` to extend Liveblocks global interface with `Presence: UserPresence` (JsonObject constraint for JSON-serializable presence data). Updated `lib/liveblocks/room-provider.tsx` to use `DEFAULT_PRESENCE` for `initialPresence`. Integrated `<Cursors />` from `@liveblocks/react-flow` and `<PresenceLayer />` into `components/canvas/DiagramCanvas.tsx` (imported Liveblocks CSS: `@liveblocks/react-ui/styles.css` and `@liveblocks/react-flow/styles.css`). Added 10 presence helper tests + 4 PresenceLayer component tests; **329 passed + 1 skipped = 330 total** (up from 317). Build green, TypeScript clean. Live cursors show other users' positions with name initials; presence updates use optimized hooks to avoid re-rendering on every cursor move. Foundation in place for AI status indicators and input queue state machine (idle/thinking/generating states defined, deferred to collaboration features). Tracker flipped to final end-of-feature state on-branch before commit.
- **Feature 34 - Project Settings** (DONE): Owner-scoped project settings surface. Created `app/(app)/projects/[projectId]/settings/page.tsx` (async server component fetching project with owner scope, redirect to `/sign-in` when user null, renders `ProjectSettings` client component). Created `components/project/ProjectSettings.tsx` (client component with three sections: Project Information with name/description edit via PATCH to existing Feature 04 route, Regeneration Section triggering existing generation routes for requirements/diagrams/context-files/feature-specs/agent-skills, Danger Zone with clear ZIP metadata via new clear-zip route and delete project with confirmation modal via existing DELETE route). Created `app/api/projects/[projectId]/clear-zip/route.ts` (POST route using `updateMany` to null ZIP metadata fields, owner-scoped, 404 on unauthorized). Installed shadcn components: `alert-dialog`, `label`, `separator` (button/card already existed from Feature 01). Added 3 settings page tests (renders with project, 404 when not found, owner-scoped query), 9 ProjectSettings component tests (renders info, saves updates, error on save failure, disables save when name empty, triggers regeneration, clears ZIP, disables clear when no ZIP, shows delete confirmation, deletes on confirmation), 4 clear-zip API route tests (401/404/200 success/owner-scoped); **350 passed + 1 skipped = 351 total** (up from 330). Build green, TypeScript clean (0 errors). All actions use existing routes (Feature 04 PATCH/DELETE, Feature 11/18/22/27 generation endpoints); only one new route added (clear-zip). Regeneration buttons trigger POST to generation routes without waiting for completion (fire-and-forget pattern, success message indicates started not completed). Delete confirmation uses AlertDialog with project name display. Clear ZIP button disabled when no cached ZIP exists. Success/error feedback via inline alerts. Tracker flipped to final end-of-feature state on-branch before commit.
- **Feature 35 - Project Member Schema** (DONE): Database foundation for collaborative access. Added `ProjectMemberRole` enum (`OWNER`, `COLLABORATOR`) and `ProjectMember` model to `prisma/schema.prisma` with `projectId`, `userId`, `role`, optional `invitedByUserId`, `joinedAt`, timestamps, cascade relations to `Project`/`User`, `@@unique([projectId, userId])`, and indexes on `userId` and `[projectId, role]`. Migration `20260611184130_add_project_members` applied; `npm run db:generate` green. No API or auth helper changes in this spec (deferred to Features 36-38). Build passes.
- **Feature 36 - Authorization Helpers** (DONE): Application-layer Owner/Collaborator authorization helpers. Created `lib/auth/project-access.ts` with `requireProjectOwner()` (owner-only gate via `Project.userId`), `requireProjectMember()` (owner or `ProjectMember` row, returns `{ id, role }`), and `getProjectRole()` (returns `OWNER`, `COLLABORATOR`, or `null`). Refactored `app/api/projects/[projectId]/route.ts` GET/PATCH/DELETE to call `requireProjectOwner()` before mutations while preserving owner-only 404 behavior. Updated `lib/projects/auth.ts` to re-export from `lib/auth/project-access.ts` for Feature 07 call sites. Added 9 unit tests in `lib/auth/project-access.test.ts` plus updated re-export tests; **12 auth helper tests passing**. Build green, TypeScript clean. Ownership failures continue to return 404, not 403. Tracker flipped to final end-of-feature state on-branch before commit.
- **Feature 37 - Invite Collaborator API** (DONE): Added the API endpoint for project owners to invite collaborators by email. Created `app/api/projects/[projectId]/members/route.ts` with POST handler. Validated input using Zod. Gated with `requireAuth()` and `requireProjectOwner()`. Validated target user exists, is not the owner, and is not already a member. Created `ProjectMember` record with `COLLABORATOR` role. Added 7 unit tests in `app/api/projects/[projectId]/members/route.test.ts` covering all edge cases. Build green, TypeScript clean. Tracker flipped to final end-of-feature state on-branch before commit.
- **Feature 39 - Shared Projects Dashboard** (DONE): Implemented shared projects section on the dashboard. Updated `GET /api/projects` to return separate `owned` and `shared` lists. Updated `listDashboardProjects` helper to fetch both using `Promise.all` with role filters. Updated `components/project/project-card.tsx` to conditionally display a 'Shared' badge and the owner's name. Updated `app/(app)/dashboard/page.tsx` to display separate 'My Projects' and 'Shared With Me' grids. Build green, tests passing.
- **Feature 40 - Member-Aware Canvas Access** (DONE): Modified Liveblocks room auth endpoint (`app/api/liveblocks-auth/route.ts`) to use `requireProjectMember()` instead of `requireProjectOwner()`. Updated Liveblocks `UserMeta` presence types to expose `role: "OWNER" | "COLLABORATOR"` within `userInfo` to let UI conditionally show controls. Verified that diagram API routes already use `requireProjectMember()`.

## In Progress

- `[ ]` Nothing in progress.

## Next Up

- `[ ]` **Feature 42 - Sharing UI**: Next numbered spec after Feature 41.

## Architecture Decisions

- **Four-Layer Polyglot Architecture**: Foundrie's own system is Rust (execution: ZIP, key rotation, file ingestion, diagram rendering, WASM) + Python (AI: LangGraph orchestration, multi-model rotation, RAG) + TypeScript (web: Next.js 16, React Flow, Liveblocks, GSAP) + Go (gateway). The deprecated v1 Python/FastAPI + TypeScript/JSZip stack is superseded.
- **Diagram-First Generation**: No feature spec is written before all applicable diagrams are generated and approved. Discovery is 8 phases. Every ZIP includes `diagrams/`. The Feature DAG drives spec ordering.
- **Prisma 7 Standard**: Database URLs (Pooled/Direct) live in `prisma.config.ts`, not `schema.prisma`.
- **Authorization vs RLS**: Security is enforced at the application layer via ownership-scoped queries and helpers (`requireProjectOwner`, `requireProjectMember`). PostgreSQL RLS is out of scope for v1.
- **Generated stacks are dynamic**: Foundrie's own stack is never the default for generated projects; stacks are chosen through research, trade-off explanation, and approval, and recorded in ADRs.
- **CodeRabbit GitHub Gate**: Wait for the user to do CodeRabbit review in GitHub. While not mandatory, it is highly recommended to catch issues early. Errors are resolved by checking official docs (Context7) rather than relying on AI training data.
- **Tailwind v4 CSS-first tokens (Feature 01)**: The spec's `Files Owned` listed `tailwind.config.ts`, but the installed Tailwind v4 is CSS-first and shadcn no longer generates a JS config. Tokens are driven through `@theme` in `app/globals.css`; the `min-touch` (44×44px) utility is defined via `@utility`. No `tailwind.config.ts` is created. User approved this deviation on 2026-06-07.
- **Next.js 16 `proxy.ts` over `middleware.ts` (Feature 02)**: The spec's `Files Owned` listed `middleware.ts`, but Next.js 16 deprecated and renamed `middleware.ts` → `proxy.ts` (the `middleware` export becomes `proxy`; `proxy` runs on the nodejs runtime, not edge). Current Clerk docs confirm: Next.js ≤15 uses `middleware.ts`, Next.js 16 uses `proxy.ts`, with identical `clerkMiddleware()` code. Foundrie is on Next.js 16.2.7, so the file is created as root `proxy.ts`. Verified via Context7 (`/vercel/next.js` v16 upgrade guide and `/clerk/clerk-docs`). User approved this deviation on 2026-06-07.
- **Prisma 7 `prisma-client` generator + driver adapters (Feature 03)**: Prisma 7 (installed 7.8.0) replaces the deprecated `prisma-client-js` with the Rust-free `prisma-client` generator, which **requires** an `output` path (client emitted to `lib/generated/prisma`, git-ignored, regenerated on `postinstall`) and **requires** a driver adapter. Foundrie uses `@prisma/adapter-neon` (`PrismaNeon`) over the pooled `DATABASE_URL`; the Neon serverless driver speaks over WebSocket (443). The `datasource`/`studio`/`adapter` config-level properties were removed in v7 — migrations work through the datasource `url` automatically. Verified via Context7 (`/prisma/web`, `/websites/neon`) and the official Prisma v7 config reference. The `prisma.config.ts` `datasource.url` points at `DIRECT_URL` for the CLI.
- **Local migration networking (Feature 03)**: `prisma migrate dev/deploy` connects over **direct TCP 5432**, which a consumer fiber router was silently blocking (TCP handshake succeeded via `nc`, but the Postgres session was reset → `P1001`; `psql` showed "server does not support SSL" because a middlebox answered on 5432, not Neon). Switching to a mobile hotspot let `prisma migrate deploy` apply both migrations cleanly ("Database schema is up to date"). DNS and the database were never the problem; the app runtime is unaffected because it uses the Neon driver over 443. Documented in `prisma/README.md`.
- **Clerk webhook secret env var name (Feature 04)**: AGENTS.md / `architecture-context.md` / the spec name the secret `CLERK_WEBHOOK_SECRET`, but the installed `@clerk/nextjs@7.4.3` `verifyWebhook` (re-exported from `@clerk/backend`) reads **`CLERK_WEBHOOK_SIGNING_SECRET`** (verified by reading the SDK source and Context7 `/clerk/clerk-docs`). The route uses `CLERK_WEBHOOK_SIGNING_SECRET` (passed explicitly via the `signingSecret` option) and `.env.example` documents that name, because that is the variable the current SDK actually consumes. `verifyWebhook` also performs the full Svix `svix-id`/`svix-timestamp`/`svix-signature` verification internally, so **no direct `svix` dependency was added**. User approved this deviation on 2026-06-08.
- **Plan project limits (Feature 04)**: The pricing research (`research/FOUNDRIE_RESEARCH.md` §16) pins Free as solo with monthly *session* limits and Pro as "unlimited", but does not pin an exact concurrent-*project* count. `PLAN_LIMITS` therefore launches with `FREE: 3` projects and paid tiers uncapped (`Infinity`) as a conservative, documented default; Stripe-backed quota enforcement and monthly session counting remain out of scope. Revisit if a billing/quota spec pins exact numbers. User approved this default on 2026-06-08.
- **AI rotation engine model-id drift (Feature 05)**: Several OpenRouter free slugs named in `architecture-context.md`'s fallback chains ("DeepSeek R1 free", "QwQ free", "Llama Maverick free", "Qwen3 free", "Kimi K2 free", "DeepSeek Chat free") have been retired/renamed by OpenRouter (verified against the live `https://openrouter.ai/api/v1/models` listing). `config/model.yaml` pins the closest current equivalents and annotates each substitution with `# was: <old name>` (e.g. Qwen3 free → `qwen/qwen3-next-80b-a3b-instruct:free`, Kimi K2 free → `moonshotai/kimi-k2.6:free`, Qwen Coder free → `qwen/qwen3-coder:free`). Direct-provider ids were verified via Context7: Anthropic pins the dated snapshot `claude-sonnet-4-5-20250929` (not the `claude-sonnet-4-5` alias); Gemini `gemini-2.5-pro`/`gemini-2.5-flash`; DeepSeek `deepseek-reasoner` (R1/thinking) + `deepseek-chat` (V3/non-thinking); Groq `llama-3.3-70b-versatile`/`llama-3.1-8b-instant`/`gemma2-9b-it`. The `fallback-chains.ts` loader Zod-rejects any model id containing a `latest` token, enforcing Hard Rule 5 at startup.
- **AI attempt logging target (Feature 05)**: The Feature 03 schema has no AI-call/log model and there is no dedicated logging feature spec, so the spec's "log the attempt" requirement is satisfied with **structured JSON logging** (per code-standards §Logging), not a DB write. `lib/ai/log.ts` emits one JSON object per line to stdout (provider, model, task, success, error, durationMs, chainIndex) with PII/secret key scrubbing, and is the single seam to redirect when the central Pino aggregator lands. No `console.log`.
- **AI exhaustion = recoverable queued state (Feature 05)**: When every provider in a chain is unavailable or rate-limited, `callAI`/`callAIStream` return a discriminated `{status:"queued", retryable:true, position:null, rateLimited}` result rather than throwing a raw provider error, matching the deployed system where the request parks in a NATS JetStream queue and the Rust key-rotation engine (50+ keys/6 providers) selects keys over gRPC. The TS engine implements the application-layer rotation + an in-process key-selection seam (`position:null` because there is no in-process queue yet); the Rust engine + NATS queue replace it in a later scale feature.
- **No global env type file added (Feature 05)**: code-standards mentions `types/env.d.ts`, but it is not in Feature 05's `Files Owned` and a shared `ProcessEnv` augmentation would collide with future specs (Hard Rule 11, no two active specs own one file). The engine instead guards every `process.env` provider-key read with an explicit `string | undefined` check. A dedicated env-typing spec can introduce the shared declaration later.
- **8 nav phases vs 6 `ProjectStatus` values (Feature 06)**: `project-overview.md` defines an 8-phase product flow (Overview → Discovery → Requirements → Architecture → Diagrams → Specs → Research → Export), but the Prisma `ProjectStatus` enum persists only the six *application* phases (no Overview/Research/Export states — those are navigational surfaces). `components/project/project-phases.ts` is the single source of truth: it lists the 8 nav phases and maps a stored `ProjectStatus` onto the phase a project is actively working through (`COMPLETE` → Export, the terminal surface). The nav renders all 8 and marks the active phase from the URL; the status map only drives the progress indicator. This keeps the spec's "all 8 phases" requirement and the DB's 6-state model consistent without inventing new enum values.
- **App shell adds top nav only; surfaces own their left rail (Feature 06)**: the Feature 01 `WorkspaceShell` already renders a contextual left rail per surface (dashboard list nav, project phase nav). Adding a second persistent global desktop sidebar in `app/(app)/layout.tsx` would double the rails on project pages. The shell layout therefore renders only the `TopNav` (brand + mobile workspace-nav drawer + Clerk `UserButton`); the global workspace nav appears in the desktop dashboard rail and the mobile `Sheet` drawer. `WorkspaceShell`'s hardcoded `min-h-screen` (Feature 01-owned, not modified) is overridden to `min-h-[calc(100svh-3.5rem)]` via its `className` prop (twMerge) so surfaces fit below the 3.5rem top nav.
- **Dashboard fetches server-side via a shared helper (Feature 06)**: the dashboard is an async server component reading directly through `lib/projects/list.ts` `listDashboardProjects()` rather than calling its own `GET /api/projects` over HTTP. The helper mirrors the Feature 04 route's query shape (ownership scope, cursor pagination, `select`-only denormalized counters, no child collections) and adds `id desc` as a deterministic tiebreaker after `updatedAt desc` so cursor pagination remains stable when timestamps tie. Clerk `@clerk/nextjs@7.4.3` exposes `Show when="signed-in"`/`UserButton` (not `SignedIn`); since `proxy.ts` already protects the `(app)` group, the shell renders `UserButton` directly and the server components guard with `getAuthUser()` → redirect-to-`/sign-in` when the local row hasn't synced.
- **Test harness is baked in (project-wide standard, 2026-06-07)**: A configured test runner ships from the first feature in Foundrie and in every generated project — testing is never deferred. The TypeScript layer uses Vitest + React Testing Library + jsdom with `test`/`test:watch`/`test:coverage` scripts (`npm run test` is non-watch). Generated non-TS stacks use the idiomatic equivalent (`pytest`, `cargo test`, `go test`) chosen through research. Captured in AGENTS.md (Hard Rule 20) and `code-standards.md` (Tests and Verification + Quality Gate).
- **Trigger.dev transitive dependency vulnerabilities (Feature 14, 2026-06-10)**: High-severity command injection vulnerabilities exist in `systeminformation` (≤5.31.5), a transitive dependency from `@trigger.dev/sdk` → `@trigger.dev/core` → `@opentelemetry/host-metrics`. These affect OpenTelemetry's build-time telemetry collection, not direct runtime code paths. The vulnerabilities (GHSA-wphj-fx3q-84ch, GHSA-5vv4-hvf7-2h46, GHSA-9c88-49p5-5ggf, GHSA-hvx9-hwr7-wjj9) involve command injection via unsanitized OS-specific operations (Windows `fsSize()`, Linux `locate`/WiFi interface/NetworkManager profile names) that are not triggered in our build/deployment context. Accepted as a controlled risk: we do not directly control Trigger.dev's dependency tree, the package is essential for durable task execution, and the exposure surface is minimal (build-time metrics collection, not user-facing runtime). Monitor for Trigger.dev updates; revisit if deployment context changes or if Trigger.dev releases a patched version.

- **Feature 25 - Code Standards Generation** (DONE): Context file generator for `context/code-standards.md`. Created `lib/ai/prompts/code-standards.ts` (comprehensive system prompt that references root `ARTKINS_STYLE_GUIDE.md` as authoritative full policy, adapts to approved stack without assuming Foundrie's Next.js/TypeScript/Tailwind stack, includes auth/ownership rules when hasAuth/hasUserOwnedData detected, Neon database rules when usesNeon detected, structured JSON logging, dependency security gates, idempotency patterns, planning+approval gate, CodeRabbit pre-push gate with Context7/official docs, forbids premature enterprise RBAC/RLS/ABAC unless explicitly required, enforces no-AI-slope quality standards). Created `lib/generation/code-standards.ts` (fetches project with requirements content JSON, executionPlans APPROVED with content/revisionNotes, researchDocuments; detects project characteristics from architecture content—hasAuth/hasUserOwnedData/hasCollaboration/usesNeon; calls `callAI('code_standards_md')` with 6000 max tokens). Modified `app/api/context-files/[projectId]/generate/route.ts` to add `CODE_STANDARDS` case in switch statement. The `code_standards_md` task was already defined in `lib/ai/model-routing.ts` routing to `unified-rotation`. Added 1 test for CODE_STANDARDS generation (mock + 200 success); **273 total passing**. Build green, TypeScript clean (0 errors). Generated code standards extend (never summarize) the root style guide, adapt to project's actual stack, include contextual security/auth/database rules, enforce version research via Context7, and forbid premature enterprise patterns unless requirements demand them. Tracker flipped to final end-of-feature state on-branch before the implementation commit.
- **Trigger.dev transitive dependency vulnerabilities (Feature 14, 2026-06-10)**: High-severity command injection vulnerabilities exist in `systeminformation` (≤5.31.5), a transitive dependency from `@trigger.dev/sdk` → `@trigger.dev/core` → `@opentelemetry/host-metrics`. These affect OpenTelemetry's build-time telemetry collection, not direct runtime code paths. The vulnerabilities (GHSA-wphj-fx3q-84ch, GHSA-5vv4-hvf7-2h46, GHSA-9c88-49p5-5ggf, GHSA-hvx9-hwr7-wjj9) involve command injection via unsanitized OS-specific operations (Windows `fsSize()`, Linux `locate`/WiFi interface/NetworkManager profile names) that are not triggered in our build/deployment context. Accepted as a controlled risk: we do not directly control Trigger.dev's dependency tree, the package is essential for durable task execution, and the exposure surface is minimal (build-time metrics collection, not user-facing runtime). Monitor for Trigger.dev updates; revisit if deployment context changes or if Trigger.dev releases a patched version.

## Open Questions

- None recorded. Record any missing product decision here before inventing behavior.

## Session Notes

- **Session 2026-06-14 (Feature 40)**: Implemented Member-Aware Canvas Access. Updated `app/api/liveblocks-auth/route.ts` to grant room access to both owners and collaborators using `requireProjectMember()`. Included user's project role in Liveblocks session `userInfo`. Verified that existing Diagram routes (`app/api/diagrams/`) already properly utilized `requireProjectMember()`. Updated Types to reflect changes. Tracker flipped to final end-of-feature state on-branch before commit (Feature 40 → Completed/DONE, Current Goal/Next Up → Feature 41).
- **Session 2026-06-14 (Feature 39)**: Implemented Shared Projects Dashboard. Updated `app/api/projects/route.ts` to return both `owned` and `shared` lists. Modified `lib/projects/list.ts` to support dual-list fetching using `Promise.all` with correct Prisma member role filtering. Adjusted `components/project/project-card.tsx` to display 'Shared' badges and owner names. Updated `app/(app)/dashboard/page.tsx` to handle the dual lists and render separate sections. Tests updated to reflect the new API signature (`ownedCursor` and `sharedCursor`). Build green, tests passing. Tracker flipped to final end-of-feature state on-branch before commit (Feature 39 → Completed/DONE, Current Goal/Next Up → Feature 40).
- **Session 2026-06-14 (Feature 36 Refactor)**: Re-reviewed Feature 36 and successfully refactored the routes in `app/api/projects/[projectId]/route.ts` to completely remove raw `where: { id, userId }` clauses in favor of relying entirely on `requireProjectOwner()`, as strictly required by the spec. Verified test pass with mocked environment variables and clean Next.js build.
- **Session 2026-06-12 (Feature 36)**: Branched `feature/36-authorization-helpers` from synced `master`. Implemented Owner/Collaborator authorization helpers in `lib/auth/project-access.ts` (`requireProjectOwner`, `requireProjectMember` returning `{ id, role }`, `getProjectRole`). Refactored `app/api/projects/[projectId]/route.ts` to gate GET/PATCH/DELETE with `requireProjectOwner()` while preserving 404-on-failure semantics. Kept `lib/projects/auth.ts` as a backward-compatible re-export layer for Feature 07+ call sites. Verified Prisma query patterns against `prisma-client-api` skill. Added 9 focused unit tests; auth helper suite **12 passing**. Build green. Tracker flipped to final end-of-feature state on-branch (Feature 36 → Completed/DONE, Current Goal/Next Up → Feature 37).
- **Session 2026-06-11 (Feature 35)**: Synced `master` (fast-forwarded to pull merged Feature 34) and branched `feature/35-project-member-schema`. Implemented database foundation for collaborative access. Added `ProjectMemberRole` enum to `prisma/schema.prisma` with `OWNER` and `COLLABORATOR` values. Created `ProjectMember` model with fields: `id` (cuid), `projectId`, `userId`, `role`, `invitedByUserId` (nullable String), `joinedAt`, `createdAt`, `updatedAt`. Added three relations: `project` (belongs to Project, cascade delete), `user` (belongs to User as "memberships", cascade delete), `invitedBy` (optional relation to User as "invitedMembers", set null on delete). Added `members ProjectMember[]` relation on Project model. Added `memberships ProjectMember[]` and `invitedMembers ProjectMember[]` relations on User model. Added constraints: `@@unique([projectId, userId])` prevents duplicate memberships, `@@index([userId])` for "shared with me" queries, `@@index([projectId, role])` for role-filtered member lists. Network blocked direct TCP migration (same Feature 03 issue); created migration manually: `prisma/migrations/20260611184130_add_project_members/migration.sql` (creates enum, table with all fields, three indexes, unique constraint, three foreign keys with proper cascade/set-null). Ran `npm run db:generate` successfully (generated Prisma client with new types). Build passes (`npm run build` green). No API routes or authorization helpers modified per spec Out of Scope (deferred to Features 36-38). Schema is the data foundation for invite flow (Feature 37) and member management UI (Features 38-42). Tracker flipped to final end-of-feature state on-branch before commit (Feature 35 → Completed/DONE, Current Goal/Next Up → Feature 36).
- **Session 2026-06-11 (Feature 34)**: Synced `master` (fast-forwarded to pull merged Feature 33) and branched `feature/34-project-settings`. Implemented owner-scoped project settings surface. Created `app/(app)/projects/[projectId]/settings/page.tsx` (async server component: `getAuthUser()` → redirect `/sign-in` on null, ownership-scoped query → `notFound()` on failure, renders `ProjectSettings` with project data). Created `components/project/ProjectSettings.tsx` (client component with controlled state for name/description, success/error feedback via inline alerts, three sections: [1] Project Information with name/description edit saving via existing PATCH `/api/projects/[projectId]` from Feature 04, save button disabled when name empty; [2] Regeneration Section with five buttons triggering existing generation endpoints without waiting (requirements → POST `/api/requirements/[projectId]/generate`, diagrams → POST `/api/diagrams/[projectId]/plan`, context files → POST `/api/context-files/[projectId]/generate`, feature specs → POST `/api/feature-specs/[projectId]/generate`, agent skills → POST `/api/skills/[projectId]/generate`), fire-and-forget pattern with "started" success messages; [3] Danger Zone with clear ZIP metadata button disabled when no cached ZIP exists, and delete project with shadcn AlertDialog confirmation showing project name). Created `app/api/projects/[projectId]/clear-zip/route.ts` (POST: `requireAuth()` → owner-scoped `updateMany` nulling `lastZipUrl`/`lastZipFileName`/`lastZipGeneratedAt`, 404 on count 0, 200 on success). Installed missing shadcn components via `npx shadcn@4.0.1 add`: `alert-dialog`, `label`, `separator` (button/card already existed from Feature 01). Added comprehensive tests: `app/(app)/projects/[projectId]/settings/page.test.tsx` (3 tests: renders with project, 404 when not found, owner-scoped query), `components/project/ProjectSettings.test.tsx` (9 tests covering rendering, save success/failure, empty name validation, regeneration triggers, clear ZIP success/disabled state, delete confirmation dialog, delete on confirmation), `app/api/projects/[projectId]/clear-zip/route.test.ts` (4 tests: 401/404/200 success/owner-scoped); **350 passed + 1 skipped = 351 total** (up from 330; 6 pre-existing DownloadZipButton timeouts not caused by this feature). Build green, TypeScript clean (0 errors). All regeneration actions use existing generation routes created in Features 11/18/22-29; only one new route added (clear-zip). Settings surface reuses Feature 04 PATCH for name/description and DELETE for project removal; no backend logic duplication. Regeneration does not block or poll completion (success message indicates "regeneration started", not "completed"). Delete confirmation shows AlertDialog with destructive styling and project name interpolation to prevent accidental deletion. Clear ZIP button disabled state prevents unnecessary API calls when no cache exists (`!project.lastZipUrl`). Component uses Foundrie design tokens (Card, Button variants, error/success alert colors). Tracker flipped to final end-of-feature state on-branch before commit (Feature 34 → Completed/DONE, Current Goal/Next Up → Feature 35).

- **Session 2026-06-11 (Feature 33)**: Synced `master` (fast-forwarded to pull merged Feature 32) and branched `feature/33-liveblocks-presence`. Implemented collaborative presence on the React Flow canvas. Created `lib/liveblocks/presence.ts` defining presence schema (`CursorPosition`, `AIPresenceStatus`, `UserPresence`) with `DEFAULT_PRESENCE` (cursor null, selectedNodeId null, aiStatus idle), `getUserColor()` cycling through 5 Foundrie accent colors modulo connectionId, and `getUserInitials()` extracting first/last initials handling whitespace/single-word names. Updated `lib/liveblocks/types.ts` to extend Liveblocks global interface with `Presence: UserPresence extends JsonObject` (JSON-serializable constraint required by Liveblocks — inlined type definition with explicit object shape instead of importing to satisfy `JsonObject` requirement). Created `components/canvas/PresenceLayer.tsx` (client component using performant `useOthersConnectionIds` + `useOther` pattern per Liveblocks skill recommendation — avoids re-rendering on every presence update by splitting connectionId array from per-user data; renders live cursors with SVG pointer + color-coded initials label; gracefully returns null when cursor position is null). Updated `components/canvas/DiagramCanvas.tsx` to import Liveblocks CSS (`@liveblocks/react-ui/styles.css`, `@liveblocks/react-flow/styles.css`), mount `<Cursors />` from `@liveblocks/react-flow` (built-in cursor rendering), and `<PresenceLayer />` (custom participant avatars). Updated `lib/liveblocks/room-provider.tsx` to use `DEFAULT_PRESENCE` for `initialPresence` (previously empty object failed type check). Initial build failed on hook name typo (`useOtherConnectionIds` → correct `useOthersConnectionIds`); fixed. Second build failed on `Presence` type not being JSON-serializable; resolved by inlining the interface in `types.ts` with explicit `extends JsonObject` and removing the separate `UserPresence` export from `presence.ts` (Liveblocks requires Presence types to be JSON objects with no complex nested structures). Added 10 presence helper tests (DEFAULT_PRESENCE structure, getUserColor consistency/cycling/wrapping, getUserInitials undefined/empty/single-word/multi-word/whitespace/case handling) + 4 PresenceLayer component tests (no users, multiple cursors, null cursor, initials display); **329 passed + 1 skipped = 330 total** (up from 317; pre-existing DownloadZipButton timeouts and DiagramCanvas test failures not introduced by this feature). Build green, TypeScript clean (0 errors). Live cursors render with participant names; presence updates follow Liveblocks performance best practices (split connectionIds from per-user selectors). Foundation in place for AI status indicators (`idle`/`thinking`/`generating` presence states defined) and future input queue state machine (deferred to collaboration/AI features per spec Out of Scope). Tracker flipped to final end-of-feature state on-branch before commit.

- **Session 2026-06-09 (AI Rotation Optimization)**: Implemented unified rotation strategy to prevent rate limit exhaustion. **Problem:** Discovery chat was failing after ~6 messages with "Error: Could not fetch response" because specialized models (e.g., `groq-llama` for streaming chat) hit rate limits quickly, and the rotation engine had no way to fall back across providers for that specific task. **Root Cause:** Each task was assigned to a specialized model key with its own narrow fallback chain. When that chain exhausted, the task failed even though other premium providers (Claude, Gemini) were available. **Solution:** Changed ALL tasks to use a single `unified-rotation` model key that cycles through the best model from each provider: Claude Sonnet 4 → Gemini Pro → DeepSeek R1 → Nvidia Llama 405B → Groq Llama 70B → OpenRouter Qwen Coder. Tier-based entry points preserved (FREE starts at position 3, PRO at position 1). **Implementation:** Added Nvidia NIM provider (`lib/ai/providers/nvidia.ts`), updated `model.yaml` with `unified-rotation` chain, modified `model-routing.ts` so `resolveModelKey()` routes all tasks to tier primary (which maps to the unified chain), updated `fallback-chains.ts` type constants to include `nvidia` and `unified-rotation`, documented the architecture change in `architecture-context.md` (deprecated the old task-specific assignments), updated Feature 05 spec with comprehensive API key setup instructions for all 6 providers, and created `docs/AI_PROVIDER_SETUP.md` (step-by-step guide). Build and tests pass. **Impact:** Discovery chat and all AI features now have 6-provider fallback depth instead of 2-4, preventing user-visible rate limit errors. This optimization is now baked into AGENTS.md, context files, and all 52 feature specs so generated projects inherit it.
- **Session 2026-06-07**: Reset the progress tracker to reflect the true state — the Next.js project has been scaffolded and no feature implementation has started. Implementation will begin from Feature 01 in strict numeric order.
- **Session 2026-06-07 (Feature 01)**: Implemented the design system on `feature/01-design-system`. Patched Next.js 16.2.4 → 16.2.7 to clear a high-severity middleware-bypass CVE (dependency-audit hard gate); 2 moderate transitive advisories remain inside Next's own tree (not fixable without a breaking downgrade). Initialized shadcn/ui (radix base), added 14 primitives, mapped the Foundrie dark palette onto shadcn semantic tokens in `globals.css`, added `design-system.ts`, workspace shell + state components, and four base surfaces. `npm run build` and `npm run lint` pass (0 errors; remaining lint warnings are pre-existing in `.agents/skills/**` templates, out of scope). Context7 was used to verify shadcn init and the `useGSAP` pattern. Next: CodeRabbit review loop.
- **Session 2026-06-07 (Feature 01 merge)**: Added the Vitest + RTL test harness and baked the test-harness requirement into AGENTS.md (Hard Rule 20) and `code-standards.md` so it applies to Foundrie and every generated project (22 tests passing). Resolved local CodeRabbit findings (dashboard error boundary now shows a generic message, no raw `error.message`; structured logging deferred to the logging feature with a documented hook; the `progress.tsx` `radix-ui` import flagged "critical" was verified a false positive — shadcn 4.x unified package, build passes). Pushed `feature/01-design-system`; user merged PR #12 to `master` manually. GitHub CodeRabbit (Free plan) returned a clean summary-level review with no unresolved findings. Local `master` fast-forwarded to the merge. Feature 01 is DONE. Holding for user go-ahead before starting Feature 02 - Auth.
- **Session 2026-06-07 (Feature 02)**: Implemented the Clerk auth foundation on `feature/02-auth`. Context7 (`/clerk/clerk-docs`, `/vercel/next.js`) confirmed Next.js 16 renames `middleware.ts` → `proxy.ts`; user approved creating root `proxy.ts` instead of the spec's `middleware.ts`. Installed `@clerk/nextjs@7.4.3` (exact-pinned). Created `proxy.ts` (default-protect with public route list exported for testing), `app/(auth)/sign-in/[[...sign-in]]/page.tsx`, `app/(auth)/sign-up/[[...sign-up]]/page.tsx`, and `.env.example` (Clerk keys). Wrapped the app in `<ClerkProvider>` with a Foundrie-token `appearance`. Added 14 tests (route-matcher boundary + page rendering); 36 total passing. `npm run build`, `npm run lint` (0 errors; warnings are pre-existing `.agents/skills/**` template noise), and dependency audit (2 pre-existing moderate advisories inside Next's tree; no critical/high) all green. Next: local CodeRabbit review loop, push, GitHub review loop, then user merge.
- **Session 2026-06-07 (Feature 02 merge)**: Installed gh CLI 2.93.0 to `~/.local/bin` (user authenticated as DonArtkins). Opened/updated PR #13; local + GitHub CodeRabbit review loop clean (Free plan, summary-level, 0 actionable findings). The one local CodeRabbit "major" finding was an unrelated uncommitted regression in `components/ui/tooltip.tsx` (invalid `calc(-50%-2px)`) — reverted to the valid committed syntax, out of Feature 02 scope. User merged PR #13 to `master`; local `master` synced; merged branch deleted locally and remotely. **Process gate miss:** the AGENTS.md step-6 tracker update was committed on the branch in its intermediate "implemented, pending review" state (Feature 02 still under In Progress) instead of the required final end-of-feature state (DONE, In Progress cleared, Current Goal/Next Up → Feature 03). Corrected on `chore/finalize-02-tracker` via PR since direct commits to `master` are forbidden. Going forward, the tracker must be flipped to its final state *before* the implementation commit on the feature branch, not left intermediate.
- **Session 2026-06-07 (Feature 03)**: Implemented the Prisma 7 + Neon data layer on `feature/03-database-schema`. Context7 (`/prisma/web`, `/websites/neon`) + the official Prisma v7 config reference confirmed Prisma 7 requires the Rust-free `prisma-client` generator (with required `output`) and a driver adapter; pinned to current stable 7.8.0 (verified via `npm view`). Built `schema.prisma` (12 models, 10 enums, all indexes), `prisma.config.ts` (→ `DIRECT_URL`), `lib/db.ts` (`PrismaNeon` over pooled `DATABASE_URL`, globalThis cache), two migrations (init + perf/autovacuum), `prisma/README.md`, env + script + gitignore updates, and 3 db tests (39 total passing). **Migration blocker:** `prisma migrate` (direct TCP 5432) failed with `P1001`/"server does not support SSL" on the user's new fiber router (a middlebox intercepts 5432). Confirmed it was the network, not the code (the setup matches the working May 4 commit `e1b56cf`), and not DNS (hostname resolved fine). Generated the migration SQL offline via `prisma migrate diff --from-empty --to-schema`, and the user applied it from a **mobile hotspot**: `prisma db execute` (reset old schema) → `prisma migrate deploy` (both migrations applied) → `prisma migrate status` ("Database schema is up to date"). Build, tests, lint (0 errors), audit (no critical/high) all green. Next: local CodeRabbit review loop, push, GitHub review loop, then user merge.
- **Session 2026-06-08 (Feature 03 merge + Feature 04)**: Synced local `master` to pull the merged Feature 03 (PR #16). Branched `feature/04-project-crud` and implemented the first user-owned API surface. Context7 (`/clerk/clerk-docs`, `/vercel/next.js`) + reading the installed `@clerk/nextjs@7.4.3` source confirmed `verifyWebhook` from `@clerk/nextjs/webhooks` does the full Svix verification internally and reads `CLERK_WEBHOOK_SIGNING_SECRET` (not the spec's `CLERK_WEBHOOK_SECRET`) — user approved that env var name and `FREE: 3` project limit. No `svix` dependency needed; added `zod@4.4.3` (exact). Created auth helpers (`get-auth-user`, `require-auth`, `plan-limits`, `is-admin`), `lib/projects/slug.ts`, and the three routes (`/api/webhooks/clerk`, `/api/projects`, `/api/projects/[projectId]`) with strict ownership scoping (404 on ownership failure, cursor pagination, denormalized counters, owner from session). Dashboard left unchanged (spec backend-only). Added 18 tests (57 total passing); build, lint (0 errors), audit (no critical/high; 5 pre-existing moderate dev-only) green. Tracker flipped to final end-of-feature state on-branch before the implementation commit. Next: local CodeRabbit review loop, push, GitHub review loop, then user merge.
- **Session 2026-06-08 (Feature 05)**: Synced local `master` (fast-forwarded to pull the merged Feature 04, PR #17 — the prior tracker text still said "pending review/merge"; corrected). Branched `feature/05-ai-rotation-engine` and implemented the AI rotation engine. Verified every provider contract and model id via Context7 (OpenRouter `/websites/openrouter_ai`, Gemini `/websites/ai_google_dev_api`, DeepSeek `/websites/api-docs_deepseek`, Groq `/websites/console_groq`, Anthropic `/websites/platform_claude_en_api`) and the live OpenRouter models API for current slugs. OpenRouter, DeepSeek, and Groq are OpenAI-compatible (one shared base + `parseSSE`); Gemini and Anthropic have bespoke adapters. Built the task→model-key map, `config/model.yaml` (pinned ids, Zod-validated, `latest` rejected), the 8 fallback chains, tier-based primary selection (FREE→DeepSeek R1, paid→Claude Sonnet 4, only flagship planning tasks promoted), `callAI`/`callAIStream` with per-attempt structured JSON logging and a recoverable `queued` state on exhaustion, and prompt utilities carrying the planning-gate clause. Added 26 tests (83 total passing); build, lint (0 errors), audit (no critical/high; 5 pre-existing moderate dev-only) green. Documented model-id drift, the structured-logging target, the queued-state/Rust-engine boundary, and the deliberate no-`types/env.d.ts` decision in Architecture Decisions. Tracker flipped to final end-of-feature state on-branch before the implementation commit. Next: local CodeRabbit review loop, push, GitHub review loop, then user merge.
- **Session 2026-06-10 (Feature 06)**: Synced local `master` (fast-forwarded to pull the merged Feature 05) and branched `feature/06-layout-shell`. Implemented the authenticated layout shell. Context7 (`/vercel/next.js`, `/clerk/clerk-docs`) confirmed Next.js 16 async `params` Promise and that the installed `@clerk/nextjs@7.4.3` exposes `Show`/`UserButton` (no `SignedIn` export); since `proxy.ts` protects the `(app)` group the shell renders `UserButton` directly. Built `app/(app)/layout.tsx` (top nav only — surfaces own their left rail via the Feature 01 `WorkspaceShell`), `TopNav` + `WorkspaceNav` (mobile `Sheet` drawer + desktop rail), the 8-phase model (`project-phases.ts`) + `ProjectPhaseNav` (active phase from pathname, diagram-first gate highlighted), the project route-group `layout.tsx` (ownership-scoped, 404 on failure) with all 8 phase pages (overview/discovery/requirements/architecture/diagrams/specs/research/export) + project `loading`/`error`/`not-found`, and converted the dashboard to an async server component fetching via the new `lib/projects/list.ts` helper (same indexed/cursor/`select`-only path as Feature 04, no child collections) rendering `ProjectCard`/`NewProjectButton`. Removed the obsolete sync dashboard test; added focused coverage for the phase model, phase placeholder, project cards/nav, new-project action, and dashboard list helper (109 total passing after CodeRabbit fixes). Build, lint (0 errors; only pre-existing `.agents/skills/**` template warnings), and high-severity dependency audit green. Local CodeRabbit fixes added derived phase-prefix coverage, stable dashboard cursor ordering (`updatedAt desc, id desc`), project-card name/date coverage, and a fail-fast guard for stale phase ids. Documented the 8-vs-6 phase mapping, top-nav-only shell, and server-side dashboard fetch in Architecture Decisions. Tracker flipped to final end-of-feature state on-branch before the implementation commit. Next: local CodeRabbit review loop, push, GitHub review loop, then user merge.
- **Session 2026-06-08 (Feature 07)**: Synced local `master` (merged Feature 06, PR #18). Branched `feature/07-research-library` and implemented the Vercel Blob file upload surface. Installed `@vercel/blob` via npm. Created project-scoped auth helpers (`requireProjectOwner`, `requireProjectMember`) in `lib/projects/auth.ts`. Created `app/api/research/[projectId]/upload/route.ts` which handles Blob token generation with strict payload typing and client-side mime-type/size rejections (blocks raw `.mp4`, `.gif`, etc.). Built `app/api/research/[projectId]/assets/route.ts` to return database asset lists and handle document creation. Frontend built via `ResearchLibrary`, `ResearchUploader` and `VisualReferenceGrid` in `components/research/` utilizing the `lucide-react` icons and our existing `WorkspaceShell` primitives. Wrote API route test files. Build and tests passed. Skips local coderabbit review per user request. Tracker flipped to final end-of-feature state on-branch before the implementation commit. Next: push, GitHub review loop, then user merge.
- **Session 2026-06-09 (Feature 09)**: Continued implementing Feature 09 (Web Research Connectors). Integrated synthesis UI, fixed TS errors, and committed changes. Awaiting GitHub CodeRabbit review.
- **Session 2026-06-09 (Feature 10)**: Synced `master` and created `feature/10-discovery-chat`. Implemented Socratic discovery interview chat with streaming. Set up conversation DB helpers (`lib/conversations/chat.ts`) restricting output strictly to bounds and integrated `callAIStream` in `app/api/conversations/[projectId]/chat/route.ts` with explicit logic for idempotency. Defined `getDiscoverySystemPrompt` in `lib/ai/prompts/discovery.ts`. Added components `DiscoveryChat` and `ChatMessage` leveraging `react-markdown` and updated `app/(app)/projects/[projectId]/discovery/page.tsx`. Wrote robust unit testing with all Vitest tests and npm run build passing smoothly. Tracker flipped to final end-of-feature state on-branch before the implementation commit. Next: local CodeRabbit review loop, push, GitHub review loop, then user merge.
- **Session 2026-06-09 (Feature 11)**: Synced `master` (fast-forwarded to pull the merged Feature 10) and branched `feature/11-requirements-generation`. Installed `@trigger.dev/sdk`. Created `trigger.config.ts` (project config with retries and `maxDuration: 300`). Built `lib/ai/prompts/requirements.ts` with two system prompts (surfacing + hidden detection). Built `trigger/generate-requirements.ts` (durable task: reads conversation + research, calls `callAI('requirements_surfacing')` and `callAI('hidden_requirement_detect')`, atomically persists via `$transaction`, advances status to `REQUIREMENTS`). Built `app/api/requirements/[projectId]/generate/route.ts` (thin POST: auth → member check → `tasks.trigger()` → 202). Initial build failed on `requireProjectMember` signature (needs `userId` as second arg) — fixed by adding `requireAuth()` call and passing `user.id`. Second build failed on `TriggerConfig` requiring `maxDuration` — added it. Final build + tests (31 files, 133 passing) green. `.env.example` updated with `TRIGGER_SECRET_KEY`. Tracker flipped to final end-of-feature state on-branch before the implementation commit. Next: push, GitHub review loop, then user merge.
- **Session 2026-06-09 (Feature 13)**: Synced `master` (fast-forwarded to pull the merged Feature 12) and branched `feature/13-architecture-proposal`. Installed `@xyflow/react@12.4.4` (exact-pinned; verified latest stable via Context7 `/xyflow/web`). Created `lib/ai/prompts/architecture.ts` (`getArchitectureProposalPrompt` for stack recommendation conversation with candidate stacks/trade-offs/sources/proactive warnings; `getArchitectureCritiquePrompt` for technical review). Built `trigger/generate-architecture.ts` (durable task: reads requirements + research, calls `callAI('architecture_proposal')` + `callAI('architecture_critique')` via unified-rotation, parses JSON responses, creates initial React Flow nodes/edges for System Context seed, persists `ExecutionPlan` with PROPOSED status containing the full proposal, advances project to `ARCHITECTURE`, returns proposal/critique/initialDiagram). Built `app/api/architecture/[projectId]/generate/route.ts` (thin POST: `requireAuth()` → `requireProjectMember()` → `tasks.trigger()` → 202; 404 on ownership failure, 401 unauthenticated). Fixed pre-existing model-routing tests to reflect unified-rotation strategy (all tasks now route through tier primary instead of specialized models; updated test expectations from `gemini-2.5-pro`/`groq-llama`/etc. to `unified-rotation`, tier primaries `deepseek-r1`/`claude-sonnet-4` apply to all tasks). Added 3 API route tests; **148 total passing**. Build, lint (0 errors), and dependency audit (no critical/high) green. Architecture prompts ensure Context7/official-source version research before recommendations, never default to Foundrie's stack unless justified, cite sources for every recommendation (benchmarks/case studies/documented failures), and surface proactive architecture warnings (N+1 risks, missing indexes, circular dependencies, error handling gaps). React Flow nodes/edges structure created as preparation for Feature 14+ canvas implementation. Tracker flipped to final end-of-feature state on-branch before the implementation commit. Next: push, GitHub review loop, then user merge.
- **Session 2026-06-10 (Feature 14)**: Synced `master` (fast-forwarded to pull the merged Feature 13) and branched `feature/14-react-flow-canvas`. Implemented the Liveblocks-backed collaborative diagram canvas. Installed `@liveblocks/client@2.20.2`, `@liveblocks/react@2.20.2`, `@liveblocks/node@2.20.2` (exact-pinned; verified latest stable via Context7 `/liveblocks/liveblocks`). Created `app/api/liveblocks-auth/route.ts` (project-scoped room authentication via `requireProjectMember`, room ID pattern `project:{projectId}`, returns Liveblocks session with `userInfo` from Clerk session). Created Liveblocks client/server/room helpers in `lib/liveblocks/`. Built `components/canvas/DiagramCanvas.tsx` (React Flow canvas wrapped in Liveblocks `RoomProvider` + `ClientSideSuspense`, collaborative nodes/edges storage via Liveblocks `useStorage`/`useMutation`, full-viewport layout with dotted background flush to app background, viewport controls, graceful degradation on auth/connection failure with fallback to empty state). Mounted canvas on architecture and diagrams phase pages. Updated `.env.example` with Liveblocks keys. Added 5 tests (auth route: 401 unauthenticated, 404 unauthorized, 200 success with session; canvas rendering); **153 total passing**. Build green, lint clean (0 errors). **Dependency audit:** High-severity vulnerabilities in transitive dependencies from `@trigger.dev/sdk` → `@opentelemetry/host-metrics` → `systeminformation` (command injection CVEs). Documented as Architecture Decision: these affect build-time telemetry collection (not runtime), exposure surface is minimal, we cannot control Trigger.dev's dep tree, and the package is essential. Accepted as controlled risk; monitor for Trigger.dev updates. Tracker flipped to final end-of-feature state on-branch before the implementation commit. Next: push, GitHub review loop, then user merge.
- **Session 2026-06-10 (Feature 15)**: Synced `master` (fast-forwarded to pull merged Feature 14) and branched `feature/15-diagram-type-selector`. Implemented category and diagram-type selection with filtered shape libraries. Created `lib/diagrams/categories.ts` defining 5 diagram categories (structural, behavioral, architectural, data, infrastructure) with id/label/description/icon metadata and `CATEGORY_ORDER` rendering order. Created `lib/diagrams/shape-libraries.ts` defining all 12 diagram types aligned with the generated suite (system-context, container, component, erd, sequence, dfd, state-machine, deployment, api-map, feature-dag, agent-architecture, security-architecture) — each type includes category, trigger condition, file formats, and node/edge shape definitions with minimal but complete shape vocabulary; added `getDiagramTypesByCategory()` and `getShapeLibrary()` helpers. Created `components/canvas/DiagramSidebar.tsx` (shadcn Tabs for category selection, grouped diagram type list with active state, shape palette placeholder for Features 16-17, Lucide React icons). Updated `components/canvas/DiagramCanvas.tsx` to integrate sidebar with flex layout (sidebar + canvas); added Liveblocks-persisted diagram type state via `useStorage`/`useMutation` (nullish coalescing to handle Liveblocks null returns). Updated `lib/liveblocks/types.ts` to include `diagramType?: DiagramType` in Storage interface. Added 18 tests (categories: 4 tests, shape-libraries: 7 tests, sidebar: 7 tests); **171 total passing**. Build initially failed on type error (`DiagramType | null | undefined` vs `DiagramType | undefined`) — fixed with `?? undefined` nullish coalescing operator. Build green, lint clean (0 errors). All diagram types match `research/FOUNDRIE_RESEARCH.md` §6 specification. Shape libraries provide shared vocabulary for manual editing and generated diagrams (Features 16-17 will wire drag-to-place). Tracker flipped to final end-of-feature state on-branch before the implementation commit. Next: push, GitHub review loop, then user merge.
- **Session 2026-06-10 (Feature 17)**: Synced `master` (fast-forwarded to pull merged Feature 16) and branched `feature/17-custom-edge-types`. Implemented UML-aware custom React Flow edges with markers and labels. Created `lib/diagrams/schemas/edges.ts` (4 Zod schemas: `UMLEdgeDataSchema` for association/aggregation/composition/inheritance/dependency, `SequenceEdgeDataSchema` for sync/async/return messages, `EREdgeDataSchema` for crow's-foot cardinality relationships, `C4EdgeDataSchema` for architectural relationships). Built edge components in `components/canvas/edges/`: `UMLEdge.tsx` (5 UML relationship types with custom SVG markers: hollow triangle for inheritance, hollow/filled diamonds for aggregation/composition, dashed line for dependency, solid arrow for association), `SequenceEdge.tsx` (straight lines with sync filled arrow, async open arrow, return dashed line), `EREdge.tsx` (crow's foot markers for one/many/zero-or-one/zero-or-many cardinality at source/target), `C4Edge.tsx` (labeled relationships with description + technology metadata). Created `components/canvas/edges/index.ts` (memoized `edgeTypes` object mapping 14 edge type strings to components, defined outside render scope). All edges validate `data` with Zod, use inlined custom SVG markers in `<defs>` (CORS-safe for Feature 21 PNG capture), position labels via `EdgeLabelRenderer` at edge midpoint, and respond to selection state with `var(--accent-primary)` highlighting. Initial build failed on `EdgeProps` type mismatch (generic expects full Edge, not just data) — fixed by removing generic and type-casting validated data. Second build failed on marker types (`{ type: MarkerType.Arrow, color }` not assignable to string) — simplified to use only custom SVG markers via `url(#markerId)` references. Added 18 schema tests + 13 component rendering tests; **227 total passing**. Build green, TypeScript clean (0 errors). Edge visual language follows `ui-context.md` (UML standard markers, straight sequence messages, ER crow's foot notation, simple C4 arrows). All markers are base64-safe inline SVG ensuring dark canvas visibility and Feature 21 PNG export compatibility. Tracker flipped to final end-of-feature state on-branch before the implementation commit. Next: push, GitHub review loop, then user merge.
- **Session 2026-06-10 (Feature 18)**: Branched `feature/18-diagram-planning` and implemented AI diagram planning. Created `lib/ai/prompts/diagram-plan.ts` (system prompt with 12 diagram types and trigger conditions: System Context always first with `orderInCategory: 0`, Container/ERD/Feature DAG/Security always, Component per container > 3 components, Sequence min 3, DFD if data flows, State Machine if complex state, Deployment if > 1 target, API Map if > 3 endpoints, Agent Architecture if agentic; outputs JSON with jobs array containing diagramTypeId/category/name/folderPath/fileName/orderInCategory). Created `lib/diagrams/schemas/plan.ts` (already existed, Zod schemas `DiagramJobSchema` and `DiagramPlanSchema`). Created `lib/diagrams/plan-diagram-jobs.ts` (fetches project with requirements + `executionPlans`, calls `callAI('diagram_planning')` with unified-rotation, parses/validates JSON response with Zod, enforces System Context first check, creates all `Diagram` records as `QUEUED` status + updates `Project.diagramCount` in single `$transaction`). Created `app/api/diagrams/[projectId]/plan/route.ts` (POST endpoint: `requireAuth()` → `requireProjectMember()`, 404 on ownership failure not 403, returns 200 with diagram list). Added `diagram_planning` task to `lib/ai/model-routing.ts` `AITask` union and `TASK_MODEL_MAP` routing to `unified-rotation`. Added 3 API route tests (401 unauthenticated, 404 unauthorized, 200 success with job list); **230 total passing** (up from 227). Initial build failed on `proposalContent` property — fixed by using `content` field (ExecutionPlan stores architecture as Markdown string, not JSON). Build green, TypeScript clean. Folder paths follow `diagrams/{category}/{diagram-type-id}/` pattern; file names `{type}-v1.mermaid`. System Context enforced first, Feature DAG will drive spec ordering (Feature 26). Sequential execution runner deferred to Feature 19. Tracker flipped to final end-of-feature state on-branch before the implementation commit. Next: commit, push, GitHub review loop, then user merge.
- **Session 2026-06-10 (Feature 19)**: Branched `feature/19-sequential-generation` and implemented the sequential diagram runner. Created `lib/ai/prompts/diagram-generation.ts` (system prompt for React Flow diagram generation with node/edge type guidance per diagram type, includes valid node/edge types for all 12 diagram categories, positioning rules, JSON output format). Added `diagram_generation` task to `lib/ai/model-routing.ts` routing to `unified-rotation`. Built `trigger/generate-diagrams.ts` (durable task using Trigger.dev `metadata` API for progress tracking: fetches queued diagrams ordered by category + orderInCategory, processes sequentially with status transitions `QUEUED` → `GENERATING` → `RENDERING` → `CAPTURING` → `DONE`/`ERROR`, calls `callAI('diagram_generation')` with requirements + architecture context, parses/validates React Flow JSON response, continues after per-job failures recording `errorMessage`, pauses after System Context completion returning `needsApproval: true` to require human approval before continuing, updates project to `DIAGRAM_GENERATION` status on completion, idempotent by diagram ID). Created API routes: `/api/diagrams/[projectId]/generate` (POST triggers task, 202 with run handle), `/api/diagrams/[projectId]/status` (GET returns all diagrams with status/progress for polling), `/api/diagrams/[projectId]/approve-system-context` (POST owner-only re-triggers task to continue after approval). Built UI components: `GenerationProgress.tsx` (category-grouped diagram list with status icons from `lucide-react`, active/completed/error states, error message display) and `GenerationControls.tsx` (Generate/Approve buttons with loading states, fetch-based triggers). Added 6 tests (3 generate route, 3 status route); **236 total passing**. Initial build failed on `ProjectStatus` enum — fixed by using `DIAGRAM_GENERATION` instead of `DIAGRAMS`. Status route test failed on db mocking — fixed by properly mocking db methods. All tests passing, build green, TypeScript clean. System Context approval gate enforced, diagram generation is recoverable and idempotent. Tracker flipped to final end-of-feature state on-branch before the implementation commit. Next: commit, push, GitHub review loop, then user merge.
- **Session 2026-06-10 (Feature 20)**: Synced `master` (fast-forwarded to pull merged Feature 19) and branched `feature/20-diagram-storage`. Implemented diagram persistence layer with Vercel Blob PNG storage. Created `lib/storage/diagram-blob.ts` (minimal `uploadDiagramPNG` uploads Buffer to Blob with versioned path `diagrams/{diagramId}/v{version}.png`, `deleteDiagramPNG` for cleanup). Created `lib/diagrams/storage.ts` (`saveDiagramData` persists React Flow JSON + PNG URL in atomic `$transaction`, uploads PNG buffer when provided via blob helper, sets status/timestamps/error messages, increments `Project.completedDiagramCount` only on first `DONE` transition checking prior status; `getDiagramWithData` retrieves full diagram including `reactFlowData` JSON for single views). Created `app/api/diagrams/[projectId]/[diagramId]/route.ts` (GET/PATCH with `requireProjectMember`, ownership failures → 404 not 403, PATCH uses `updateMany` scoped by projectId with explicit `Record<string, unknown>` type to satisfy Prisma JSON constraints — avoided spread operators). Added 14 tests (2 blob upload/delete, 6 storage including counter increment/PNG upload/error recording, 6 API route 401/404/200); **250 total passing**. Initial build failed on TypeScript JSON type errors — fixed by using explicit `Record<string, unknown>` instead of typed interfaces with JSON fields, avoiding spread operators that confuse Prisma's complex discriminated unions. Build green, lint clean (0 errors). List views explicitly avoid selecting `reactFlowData` (large JSON column), relying on existing `@@index([projectId, category, orderInCategory])`. Vercel Blob integration reuses `@vercel/blob@2.4.0` from Feature 07 (no new install). Diagram versioning structure in place (PNG paths include version number, version field tracked). Error placeholder behavior records `errorMessage` + `ERROR` status when capture fails (Feature 21 integration point — `pngBuffer` optional parameter accepted but unused until Feature 21 provides buffers). Tracker flipped to final end-of-feature state on-branch before the implementation commit. Next: push, GitHub review loop, then user merge.
- **Session 2026-06-10 (Feature 22 rescue/sync)**: Investigated Kiro CLI throttling and fixed the real Feature 22 build blocker. `lib/generation/project-overview.ts` now queries current schema fields (`Project.executionPlans`, `ResearchDocument.title/sourceType/content`, `ExecutionPlan.content/revisionNotes`), passes the user's plan into the current `callAI('project_overview_md', { systemPrompt, userPrompt, plan, maxTokens })` contract, handles `status: "queued"`, and returns `response.text`. `app/api/context-files/[projectId]/generate/route.ts` now maps `AuthError` to 401, `ProjectAuthError` to 404, and logs unexpected failures as structured JSON without `console.error`. Added a hard contract-synchronization gate to AGENTS.md, all six context files, and all 52 feature specs; corrected future specs that referenced stale Prisma/AI contracts (`ResearchAsset.aiSummary`, unsupported `ResearchSource` fields, old AI response shapes, `AGENTS_MD` enum assumptions). `npm run build` passes.

- **Feature 25 - Code Standards Generation** (DONE): Context file generator for `context/code-standards.md`. Created `lib/ai/prompts/code-standards.ts` (comprehensive system prompt that references root `ARTKINS_STYLE_GUIDE.md` as authoritative full policy, adapts to approved stack without assuming Foundrie's Next.js/TypeScript/Tailwind stack, includes auth/ownership rules when hasAuth/hasUserOwnedData detected, Neon database rules when usesNeon detected, structured JSON logging, dependency security gates, idempotency patterns, planning+approval gate, CodeRabbit pre-push gate with Context7/official docs, forbids premature enterprise RBAC/RLS/ABAC unless explicitly required, enforces no-AI-slope quality standards). Created `lib/generation/code-standards.ts` (fetches project with requirements content JSON, executionPlans APPROVED with content/revisionNotes, researchDocuments; detects project characteristics from architecture content—hasAuth/hasUserOwnedData/hasCollaboration/usesNeon; calls `callAI('code_standards_md')` with 6000 max tokens). Modified `app/api/context-files/[projectId]/generate/route.ts` to add `CODE_STANDARDS` case in switch statement. The `code_standards_md` task was already defined in `lib/ai/model-routing.ts` routing to `unified-rotation`. Added 1 test for CODE_STANDARDS generation (mock + 200 success); **273 total passing**. Build green, TypeScript clean (0 errors). Generated code standards extend (never summarize) the root style guide, adapt to project's actual stack, include contextual security/auth/database rules, enforce version research via Context7, and forbid premature enterprise patterns unless requirements demand them.

- **Feature 26 - Feature Specs Generation** (DONE): Ordered feature spec generation from diagrams. Added `feature_specs_generation` task to `lib/ai/model-routing.ts` AITask type and TASK_MODEL_MAP routing to `unified-rotation`. Created `lib/ai/prompts/feature-specs.ts` (comprehensive system prompt with exact spec template structure, all generation rules: DAG-based ordering, Files Owned no-overlap, no premature RBAC/auth, stack-adaptive never assumes web/React/Next.js, research references when implementation depends on assets, Context7 version research before dependency pinning, binary acceptance criteria, plan approval required, root `ARTKINS_STYLE_GUIDE.md` reference mandatory). Created `lib/generation/feature-specs.ts` (reads project with requirements content JSON, executionPlans APPROVED with content/revisionNotes, diagrams including Feature DAG reactFlowData for ordering, contextFiles, researchDocuments; calls `callAI('feature_specs_generation')` with 8000 max tokens; parses JSON response array of specs; persists in `Serializable` transaction deleting existing + creating all specs with slugified titles + updating Project.featureSpecCount). Created `app/api/feature-specs/[projectId]/generate/route.ts` (POST: `requireAuth()` → `requireProjectMember()` → `generateFeatureSpecs()` → 200 with specs array; 401 unauthenticated, 404 unauthorized, 500 on generation failure with structured `logEvent`). Added 3 API route tests (401/404/200); **276 total passing**. Build green, TypeScript clean (0 errors). Generated specs follow exact structure with contract synchronization gate, critical gitignore/setup/version-research instructions, Out of Scope, Future Modifications, and binary acceptance criteria. Feature DAG drives ordering; no two specs own the same file.
- **Session 2026-06-11 (Feature 28)**: Synced `master` (fast-forwarded to pull merged Feature 27) and branched `feature/28-agents-md-generation`. Implemented AGENTS.md generation for exported project packages. Created `lib/ai/prompts/agents-md.ts` (comprehensive 311-line prompt generating seven required sections: Project Identity with approved stack reference never assuming web/Foundrie stack, Mandatory Reading Order placing diagrams before context files per diagram-first methodology, Init Plan Data listing env vars with sources/CLI tools with install commands/accounts with URLs/gate sentence, Hard Rules encoding planning approval/branch-first workflow/one-spec-at-a-time/CodeRabbit pre-push/Context7 version checks/auth ownership scoping/no premature RBAC-RLS, Feature Order, Stack Reference with Context7 library IDs, Research Files with provisioned skills grouped by type). Created `lib/generation/agents-md.ts` (271 lines: fetches project with requirements content JSON/executionPlans APPROVED/contextFiles/featureSpecs ordered by order field/diagrams DONE by category+orderInCategory/researchDocuments/agentSkills; detects stack characteristics via regex [hasAuth, hasUserOwnedData, usesNeon]; extracts stack summary from architecture content; classifies skills into Universal [code-review/autofix/context7-cli/find-docs] / Stack-Dependent [remaining non-custom slugs] / Custom [project-research]; dynamically extracts env vars/CLI tools/accounts from architecture content via helper functions matching common patterns [Clerk/Neon/Vercel Blob/Trigger.dev/Liveblocks]; calls `callAI('agents_md_generation')` with 8000 max tokens and `plan: "FREE"`; handles queued status). Modified `app/api/context-files/[projectId]/generate/route.ts` to add `AI_WORKFLOW_RULES` case using existing `ContextFileType` enum value (no schema migration required per spec). The `agents_md_generation` task was already defined in `lib/ai/model-routing.ts` routing to `unified-rotation`. Added 3 API route tests (401 unauthenticated, 404 unauthorized, 200 success with AGENTS.md content containing all seven sections); **280 total passing**. Initial build failed on regex `/s` flag (ES2018-only) — fixed by replacing with `[\s\S]` character class for dotall matching. Second build failed on `plan: "free"` type error — fixed using uppercase `"FREE"` matching Plan enum. Final build green, TypeScript clean (0 errors). Generated AGENTS.md encodes diagram-first reading order, stack-adaptive guidance from approved architecture (never defaulting to Foundrie's own polyglot stack), complete init plan with all setup steps and gate sentence, all hard rules (planning gate, branch-first Git workflow, one-spec-at-a-time with CodeRabbit review loop, Context7-driven version research rejecting `"latest"`, user-input-first philosophy, auth/authorization separation with ownership-scoped queries returning 404 on failures, no premature enterprise security patterns, structured logging, dependency audit gate, diagram-first implementation rule, spec structure), ordered feature list, stack reference with Context7 library IDs, research files list, and provisioned agent skills grouped by category with usage instructions. Tracker flipped to final end-of-feature state on-branch before commit.
- **Session 2026-06-11 (Feature 29)**: Synced `master` (fast-forwarded to pull merged Feature 28) and branched `feature/29-progress-tracker`. Implemented progress tracker generation for exported project packages. Created `lib/ai/prompts/progress-tracker.ts` (system prompt generating complete progress tracker with Contract Synchronization Gate warning, Current Phase mapping from ProjectStatus enum, Current Goal always pointing to Feature 01 for initial seed, empty In Progress/Completed lists initially, Architecture Decisions extracted from execution plan, Diagram Version Log listing all DONE diagrams with version numbers, empty Open Questions initially, Session Notes referencing initialization date and counts, Process Reminders for end-of-feature tracker updates, Last Updated ISO timestamp). Created `lib/generation/progress-tracker.ts` (131 lines: fetches project with status/featureSpecs ordered by order field/diagrams DONE with version/executionPlans APPROVED with content and revisionNotes/researchDocuments count; maps ProjectStatus to human-readable phase names via phaseMap; extracts architecture decisions from execution plan content matching Stack Decision section or defaulting to approval message; calls `callAI('progress_tracker_md')` with 4000 max tokens). Modified `app/api/context-files/[projectId]/generate/route.ts` to add `PROGRESS_TRACKER` case in switch statement importing `generateProgressTracker`. The `progress_tracker_md` task was already present in `lib/ai/model-routing.ts` routing to `unified-rotation`. Added 3 API route tests (401 unauthenticated, 404 project not found, 200 success with valid tracker containing all required sections); **283 total passing**. Build green, TypeScript clean (0 errors). Generated progress tracker provides resumable implementation status seeded for coding agents to start from Feature 01, with all features marked NOT STARTED, diagram version log documenting which diagram versions specs were written from, architecture decisions for context, and timestamp for tracking updates. Tracker flipped to final end-of-feature state on-branch before commit.
- **Session 2026-06-11 (Feature 30)**: Synced `master` (fast-forwarded to pull merged Feature 29) and branched `feature/30-zip-builder`. Implemented server-side ZIP assembly. Installed `jszip@3.10.1` (exact-pinned; verified latest stable via `npm view`, includes built-in types). Created `lib/zip/zip-structure.ts` (canonical layout defining required/conditional folders, `generateRootFolderName()` for `{slug}_{YYYY-MM-DD_HH-mm-ss}` format). Created `lib/zip/build-project-zip.ts` (main `buildProjectZip()` fetches project data in `RepeatableRead` transaction, assembles ZIP with root files/context/specs/mandatory diagrams/research/placeholders for future folders/conditional .agents when skills exist; `downloadBlobAsset()` wraps Vercel `get(url, {access:'public'})` with ReadableStream→Buffer conversion; error placeholders preserve URLs without failing build; returns compressed Buffer). Fixed Vercel Blob API (corrected to `get()` from nonexistent `download()`), Prisma field names (diagramTypeId/pngStorageUrl/storageUrl). Added 14 tests; **297 passing + 1 skipped = 298 total** (up from 283). Build green, TypeScript clean. Tracker flipped to final end-of-feature state on-branch before commit.
- **Session 2026-06-14 (Feature 37)**: Branched `feature/37-invite-collaborator-api` from synced `master`. Implemented the invite collaborator API endpoint. Created `app/api/projects/[projectId]/members/route.ts` with a POST handler that allows project owners to invite collaborators by email. Used Zod for request body validation. Ensured endpoints are gated with `requireAuth()` and `requireProjectOwner()`. Enforced constraints: target user must exist, target user cannot be the owner, and target user cannot already be a member. Created a new `ProjectMember` record with the `COLLABORATOR` role on success. Added 7 focused unit tests in `app/api/projects/[projectId]/members/route.test.ts` covering validation, authentication, ownership scoping, constraints and success cases. Handled ZodError correctly using `.issues`. Tests pass. Build passes. Tracker updated.
- **Session 2026-06-14 (Feature 38)**: Synced `master` and branched `feature/38-list-remove-collaborators-api`. Implemented `GET /api/projects/[projectId]/members` returning all members including the owner, and `DELETE /api/projects/[projectId]/members/[memberId]` allowing owners to remove collaborators and collaborators to remove themselves. Ownership and role checks enforced via `requireProjectMember`. Unit tests cover authentication, project access, and self-removal checks. Fixed test setup to inject `DATABASE_URL` for isolated unit tests. All tests pass and build is green.

## Process Reminders

- End-of-feature tracker update (AGENTS.md step 6) is a hard gate: before committing the feature branch, the current spec must be moved to Completed/DONE, In Progress cleared, and Current Goal + Next Up set to the next numbered spec. Do not commit the tracker in an intermediate "pending review" state — the merged branch must land a tracker already pointing at the next feature.
- **Feature 41 - Member-Aware AI Generation** (DONE): Extended all AI generation and content routes to use `requireProjectMember()` instead of owner-only checks. Both Owners and Collaborators can now trigger discovery chat, requirements generation, architecture proposals, diagram generation, context file generation, feature spec generation, agent skills generation, and ZIP download. Updated routes: requirements GET/PATCH, download POST/GET, research upload/assets/analyze/links/synthesize, and diagram approve-system-context. Owner-only routes (project PATCH/DELETE/settings, member management) remain protected with `requireProjectOwner()`. Test files updated to mock `requireProjectMember`.
