/**
 * @deprecated Import from `@/lib/auth/project-access` instead.
 * Re-exported for backward compatibility with Feature 07 call sites.
 */
export {
  ProjectAuthError,
  getProjectRole,
  requireProjectMember,
  requireProjectOwner,
  type ProjectAccessResult,
} from "@/lib/auth/project-access";
